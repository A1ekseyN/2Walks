--- Известные баги: ---
Критичные:
- [FIXED 0.2.0k, task 2.1] Новый день, если не сохранить данные, тогда можно будет использовать накопленные за вчера шаги. (Было через save.txt; теперь единственный источник правды — state.date_last_enter.)
- Какая-то шляпа при обновлении кол-ва шагов нового дня. Вроде, все работает, но что-то нужно изменить. Пока не понятно, что именно.
- [FIXED 0.2.0m, task 2.4] При обновлении Бонуса, за пройденные 10к шагов, в течении прошлого дня, обнуляется бонус. (Перед rollover'ом дня в save_game_date_last_enter теперь max-merge state.steps.today из steps_log за уходящий день — yesterday получает максимально полное значение независимо от того, был ли game_state snapshot обновлён.)
- [FIXED 0.2.0a, task 2.3] В момент запуска игры у нас создается класс Adventure для приключений. Если у нас прокачается навык во время запущенной игры, то изменения не применятся для приключений. (Adventure теперь пересоздаётся каждую итерацию main loop'а с актуальным state; drop.py:luck_chr заменён на current_luck(state).)

Не критичные:
- Работа. Добавить проверку кол-ва энергии необходимые для начала работы.
- [FIXED 0.2.0a, task 2.2.1] При добавлении energy идет округление по round. И иногда округляется 1.6 = 2. (Мёртвый round() убран в energy_time_charge — `(a-b)//c` уже floor-деление.)
- [FIXED 0.2.0a, task 2.6] Когда персонаж на работе, и войти в меню конкретной работы (К примеру: Сторож), то обнуляется ЗП = 0. (Запись актуальна для версий до 0.2.0a — хранение salary было через словарь char_characteristic, при загрузке из неполного CSV salary мог остаться 0. После миграции на dataclass state.work + state.from_dict(int(d.get('work_salary', 0))) и Work.check_requirements который всегда задаёт salary из work_requirements[type]['salary'] — баг не воспроизводится. Подтверждено воспроизведением 04.05.2026: меню add_working_hours корректно показывает salary*hours.)

+ [FIXED 0.2.0a, task 2.2.2 / 0.2.0l, task 2.2.3] При запущенной программе и макс. эн. накапливается время, и не происходит отнимание энергии при первом переходе. ("Бесплатная энергия после максимума": 2.2.2 закрыл частный случай — sync стампа в energy_time_charge при energy>=energy_max; 2.2.3 закрыл полный — actions.try_spend синкает стамп после траты, если перед ней энергия была на full.)
+ [FIXED 0.2.0a, task 2.2.1] Не правильно считается время в формуле энергии, которое прошло более 60-ти секунд. (Strict `>` заменено на `>=`, неатомарные timestamp_now() сведены в один `now`.)

