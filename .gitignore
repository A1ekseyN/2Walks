tasks.txt

/credentials/

# Python compile artifacts (matches at any depth — root, tests/, web/, etc.).
__pycache__/
*.pyc

# macOS Finder metadata.
.DS_Store

# Legacy day-rollover marker (removed in 0.2.0k, task 2.1).
# May still be created by older code revisions — keep gitignored.
save.txt

# History log (4.6) — личные данные игрока, append-only JSONL.
history.jsonl

# Player save state — личные данные, аналогично history.jsonl.
# Sheets — primary source of truth, локальный файл пишется на каждый
# save_characteristic() для offline-fallback. Заводится автоматически
# при первом `s` в CLI после успешного init_game_state().
# - state.json — primary с 1.4.3 (0.2.5)
# - characteristic.csv — legacy CSV fallback, удалится в 1.4.3.1
state.json
characteristic.csv

# Backup snapshots от scripts/backup_sheets_before_migration.py (1.4.3).
backup_before_*.json
