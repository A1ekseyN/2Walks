# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project context

2Walks is a step-counter RPG written in Python. Real-world steps (entered manually via the `+` command) fuel in-game actions: training at the Gym, Work shifts, Adventures with item drops, deposits / loans at the Bank, etc. Comments and UI text are primarily in Russian. The project targets desktop (Mac); an Android/Kivy build existed historically but was removed — see `git log` before 2026-04-24 if you need to resurrect any of it. A Google Fit auto-sync existed historically but was removed on 2026-04-27 (task 4.16); iPhone Shortcut pipeline (4.13) is currently отложено — entry will be via CLI / Web / API (task 4.48.2 `POST /api/steps`).

**Primary interface:** CLI (`game.py`). **Secondary (planned):** Web interface via FastAPI backend on a VPS (task 4.48 — incremental rollout). CLI remains the primary path; web is supplementary and grows feature-by-feature. Single source of truth for both — Google Sheets.

## Entry points

A single runnable root:

- `game.py` — pure CLI REPL (`location_selection()` loop). Also re-exported as a function `game()` for programmatic use.

## Common commands

```bash
# Run the game (CLI)
python game.py

# Run the web interface (FastAPI, dev mode). Requires venv active or call .venv/bin/uvicorn directly.
source .venv/bin/activate && uvicorn web.main:app --reload --host 127.0.0.1 --port 8008

# Show current Sheets game_state contents (debugging)
python google_sheets_db.py

# One-time migration of Google Sheets layout (renames Sheet1 → game_state, creates steps_log)
python migrate_sheets.py

# Drop-rate Monte-Carlo simulation (5 luck × 7 difficulty × 10k iterations on real Drop_Item)
python drop_test_montecarlo.py

# Static type check (mypy with permissive baseline — see mypy.ini)
mypy .
```

Dev-only deps (mypy и т.д.) живут в `requirements-dev.txt` — установка `pip install -r requirements-dev.txt`. Основные runtime-зависимости — `requirements.txt`.

CLI и web — отдельные процессы, у каждого свой `game.state`. В MVP запускаем только что-то одно за раз; sync resolution CLI ↔ Web — задача 4.54.

Подробная шпаргалка по локальному запуску (включая доступ с iPhone через домашнюю Wi-Fi) — [`docs/local_setup.md`](docs/local_setup.md).

Pytest is the test framework (config in `pytest.ini`, tests in `tests/`). Run all tests with `.venv/bin/pytest tests/`. There is no linter or CI configured. `drop_test_montecarlo.py` is a standalone Monte-Carlo drop simulator — uses the **real** `Drop_Item.one_item_random_grade(hard, state)` from `drop.py` (no fork-copy after 3.2.2 / 0.2.1f), so its measurements correspond to the actual game balance and are usable for tuning `drop_percent_*` constants.

## Architecture

### Game state: `GameState` (state.py)

The single source of game state is a `GameState` dataclass in `state.py` with nested subclasses (`StepsState`, `CharLevel`, `GymSkills`, `TrainingSession`, `WorkSession`, `AdventureSession`, `Equipment`, `BankState`). The live instance is held by a container `game = _GameContainer()` in `characteristics.py` and accessed as `game.state` — populated by `init_game_state()` (task 1.2). Most gameplay modules take `state: GameState` as an explicit parameter — there is no implicit global. Mutate `state.<sub>.<field>` to communicate between systems.

A historical legacy dict `char_characteristic` and proxy class were removed in version 0.2.0 (task 1.1). If you encounter that name in old commits or external docs, it referred to today's `game.state`.

**Initialization is now lazy** (task 1.2, version 0.2.0c). Importing `characteristics` does NOT touch Google Sheets — only the call to `init_game_state()` does. CLI calls it from `__main__` before `play()`; FastAPI (task 4.48) will call it from a startup hook. Tests construct their own `GameState` and never need init at all, so the test suite runs in ~1 sec.

### Persistence layers (three of them)

Saves are written to **both** on save; loads prefer Google Sheets with CSV fallback:

1. `characteristic.csv` — flat CSV (offline fallback). `save_characteristic()` writes `game.state.to_dict()`; `load_characteristic()` parses back via `ast.literal_eval` for nested dicts/lists. `GameState.from_dict()` then reconstructs the dataclass. Datetime keys (`skill_training_time_end`, `working_end`, `adventure_end_timestamp`) are parsed via `%Y-%m-%d %H:%M:%S.%f`. Раньше существовал ещё третий формат — `characteristic.txt` (JSON mirror) — но он был write-only zombie (нигде не читался) и удалён в 0.2.3c (задача 1.4.1). Дальнейшая унификация Sheets/CSV — задачи 1.4.2 (общий parser) и 1.4.3 (JSON-blob в Sheets).
2. Google Sheets — three specialized worksheets in one spreadsheet (task 4.14 + 4.6, version 0.2.0d / 0.2.4):
    - `game_state` — full state snapshot (Key/Value layout). Renamed from legacy `Sheet1`.
    - `steps_log` — append-only log of step measurements (`ts | user_id | steps | source`). `ts` is Unix timestamp (`float`); `source` is `'manual'` (CLI), `'web'` (web form / `POST /api/steps`), `'auto'` (future iPhone Shortcut, currently отложено). Used by max-merge (`apply_steps_log_max_merge`, task 4.15) — applied on every load (CLI start + web reload) so any input channel reflects on next start, even if the `game_state` snapshot is stale.
    - `history` — append-only лог значимых игровых событий (task 4.6, since 0.2.4). Колонки: `ts | datetime | user_id | game_version | event_type | payload_json`. Dual-write: local `history.jsonl` (primary, sync, fail-resistant) + Sheets `history` (best-effort, fail-silent через `HistoryLogRepo.append`). Sheets копия для будущих dashboard'ов (4.10) и multi-device access. На каждое значимое событие (work_done, skill_upgraded, deposit, drop, level_up, item_bought / sold, etc. — 24 типа после 0.2.4c, +5 drop-related: drop_pending, drop_force_sold, drop_auto_collected, drop_resolved_sell_existing, drop_resolved_sell_new — задача 4.50.1) вызывается `history.log_event(type, **payload)`. Schema event'а с версионированием: `{v, ts, date, time, user_id, game_version, type, payload}` — `v` для будущей миграции, `game_version` для отладки (понять какие фичи были на момент события). Pruning / CLI viewer / re-sync after Sheets recovery — отложенные follow-up подзадачи (4.6.1 / 4.6.2 / 4.6.3). Pytest fixture `tests/conftest.py` с autouse отключает `log_event` для всех тестов кроме `test_history.py` — иначе тесты загрязняют реальные local file и Sheets.

Web dashboard refresh model (after 0.2.0j): no automatic HTMX polling. Numbers update on F5 / pull-to-refresh (full reload triggers `try_reload_state()` which fetches Sheets + applies max-merge) or after form submit (`POST /web/steps`, `POST /web/work/*` etc. — endpoints return updated fragment, HTMX swaps `#status-bar`). Active session timers (training / work / adventure) tick every second via JS without server roundtrips. The `GET /status` endpoint remains in code for future use (Refresh button — 4.54.0.1, action endpoints — 4.48.3+).

Dashboard layout (compact / mobile-first): the always-visible Stats area shows only current numbers (Steps with click-to-input form, Energy, Money, Level + progress). Section order: Stats → ⏱ Активные сессии (only if any active) → 🏭 Работа → 📈 Бонусы → 🧥 Экипировка → 🎒 Инвентарь. Four sections are wrapped in native HTML5 `<details>` and collapsed by default — click summary to expand:
- **🏭 Работа** — summary shows current shift inline (`watchman · 8 $ (4 ч)`) when active, or "выбери вакансию" otherwise. Inside: 4-vacancy menu with hour buttons (`1ч..Nч`, cap 8) when not working, or `+Nч` add-hours form when working. Collapsed in BOTH states (working and not) — Stats stays the primary view.
- **📈 Бонусы** — Steps breakdown (stamina · equipment · daily · level + total/percent), Energy breakdown, Total used.
- **🧥 Экипировка** — summary shows worn count `(N/7)` + non-zero equipment bonuses inline (zero values filtered); inside — slot-by-slot list.
- **🎒 Инвентарь** — summary shows item count `(N/cap)`; inside — sorted item list. Cap = `bonus.backpack_capacity(state)` = `BASE_BACKPACK_CAPACITY (10)` + `state.gym.backpack_skill` (см. 4.50.0). При active `state.pending_drop` (4.50.2) details auto-expand'ит (`open` атрибут), на каждой строке появляется кнопка «Продать + взять находку» (`POST /web/drop/sell_existing` с hidden index). Над всеми секциями рендерится отдельный pending-баннер с описанием находки + кнопками «Продать находку» / «Отложить» (`POST /web/drop/sell_new` / `POST /web/drop/skip`). API-зеркала: `POST /api/drop/sell_existing` (JSON `{index}`) и `POST /api/drop/sell_new` (JSON `{}`).

Collapsed content stays in DOM (accessibility / search-friendly), only visually hidden by Pico.css default styles. After HTMX swap (any `POST /web/*` endpoint) sections close — by design (no state persistence between renders).

   Access via `google_sheets_db.GameStateRepo` (save/load) and `StepsLogRepo` (append/for_day) classes. A lazy singleton `_get_client()` keeps one authorized gspread client per process. New deployments need a one-time `python migrate_sheets.py` to rename `Sheet1` and create `steps_log`.

   **Locale-safe float serialization (since 0.2.3g):** Sheets API без `value_input_option=RAW` интерпретирует числовые значения по системной локали. На локали с запятой как десятичным разделителем (ru-RU и т.п.) float `2018.10` сохраняется как `'2018,1'`, на чтении `ast.literal_eval('2018,1')` → tuple `(2018, 1)` → crash в `from_dict: float(tuple)`. Фикс: (1) `_state_dict_to_rows` конвертирует float в строку через `repr()` ('2018.1' с точкой); (2) `GameStateRepo.save` использует `value_input_option=ValueInputOption.raw`; (3) `StepsLogRepo.append` — то же `RAW` для `ts: float`. Без этого фикса все float-поля (money, bank_*, steps_xp_bonus, timestamps) ломаются на любой локали кроме en-US. При написании новых float-полей или новых call-sites в Sheets — **обязательно использовать RAW** (через `value_input_option=ValueInputOption.raw` в `update`/`append_row`).

   Steps_log writes happen only on explicit save (`s` or `q`) — not on every `+N` input — to keep offline-mode usable (player can enter wrong steps and exit without save).

When adding new fields to `GameState`, update both `from_dict` and `to_dict` so the round-trip stays clean (the datetime special-case list in `_deser_datetime` / `json_serial` is the common trap).

### Step-count input

`state.steps.today` is set by one of three input channels:
1. **CLI**: `+N` command in main menu invokes `steps_today_manual_entry(state)` — `max(current, entered)`.
2. **Web form**: clickable Steps row on dashboard → `POST /web/steps` (form-data) → `_apply_new_steps()`.
3. **API**: `POST /api/steps` with JSON `{"steps": int}` — same helper, used by curl / future iPhone Shortcut.

All three write a row to the `steps_log` Sheet; web/API additionally update `state.steps.today` in memory immediately. The `apply_steps_log_max_merge()` helper (task 4.15) runs on every load (CLI start via `init_game_state`, web reload via `web.sync.try_reload_state`) and bumps `state.steps.today` to the maximum of all log entries for today — so input from any channel becomes visible on next start even if the `game_state` snapshot in Sheets is stale.

On date change (`save_game_date_last_enter(state)`), `today` and `used` reset to `0` so the new day starts fresh; the player re-enters the bracelet reading via any channel. The single source of truth for day rollover is `state.date_last_enter` (compared against `datetime.now().date()`); legacy `save.txt` was removed in 0.2.0k (task 2.1).

### Web mutation endpoints (4.48.5+)

Web actions that change state (currently Work — start shift / add hours; later training and adventure) follow a common pattern:

1. **Validate + apply via shared helper.** Each subsystem has a `_validate_and_apply_<thing>(state, ...) -> Optional[str]` helper in `web/main.py` that does range/resource checks first, then calls the existing CLI mutation path (`Work.check_requirements()` for Work — which under the hood does `actions.try_spend` + `actions.start_work`), and finally calls `persist_state_to_cloud()` on success. Returns error text or None.
2. **Persist after every successful mutation.** `persist_state_to_cloud()` (in `web/sync.py`) runs `save_characteristic()` (CSV+JSON, guaranteed) followed by `GameStateRepo().save(state.to_dict())` (Sheets, best-effort — Sheets failure is logged but does not block the endpoint). Without this step, mutations would live only in uvicorn RAM and be lost on restart / invisible to CLI. Steps (`_apply_new_steps`) are the exception — they write to the append-only `steps_log` and rely on max-merge for resync, so they don't need a full snapshot save (except after a rollover — see below).
3. **Two endpoints per action: Form + JSON.** `POST /web/<action>` accepts form-data and returns the rendered `_status_fragment.html` for HTMX swap. `POST /api/<action>` accepts JSON via Pydantic models (e.g. `WorkStartRequest`, `WorkAddHoursRequest`) and returns JSON `{ok, work: {...}}` or `{ok: false, error}`. Both share the same validate/apply helper.
4. **Auto-finalize timer-based sessions.** Finalizers like `work_check_done(state)` (and future training / adventure analogs) are called at the top of `_dashboard_context()` so any GET / POST to web automatically closes finished sessions and credits rewards — no separate "Claim" click needed in web. CLI still calls these in its main loop tick.
5. **No mid-session vacancy switch.** Work is intentionally locked to the chosen vacancy until the shift ends (matches CLI). `add_hours` uses `state.work.work_type` from state rather than accepting it from the request.
6. **Day rollover (4.54.0.2).** `save_game_date_last_enter(state)` is called from three places: (a) `try_reload_state` after `update_from_dict` — primary trigger on F5 / pull-to-refresh; (b) `_dashboard_context` — defense-in-depth for HTMX swap chains that don't go through reload; (c) `_apply_new_steps` — covers `POST /api/steps` from iPhone Shortcut without prior GET /. After an actual date change, `persist_state_to_cloud()` is called so CLI and web see the same snapshot. Active sessions (`state.work` / `state.training` / `state.adventure`) are NOT touched on rollover — timers run through midnight as in CLI (a 23:30 → 01:30 work shift completes naturally on the next day).
7. **Energy auto-regen (0.2.1c).** `energy_time_charge(state)` is called in `_dashboard_context` on every render — same helper CLI uses on each main loop tick. State.energy grows in uvicorn RAM at the rate `+1 / speed_skill_equipment_and_level_bonus(60, state)` seconds. NO persist — energy changes too often, writing every render would burn Sheets quota for no benefit; next real mutation (work start / steps) syncs the actual snapshot. Client-side: a JS `tickEnergy()` runs every 60 seconds (matches the regen rate), reads `data-energy-stamp` / `data-energy-interval` attributes, recomputes `min(energy + floor((now - stamp) / interval), max)` on the fly. At max — number stays static (no special UI marker).
8. **Level-up auto-detection (0.2.1d).** `CharLevel(state).update_level()` is called in `_dashboard_context` on every render. Without this, web-only players never crossed level thresholds (CLI's `level_status_bar` was the only caller). Persist only when `state.char_level.level` actually changed — same-level renders are no-ops. Skill point allocation (`POST /web/level/allocate` / `POST /api/level/allocate`) is the partner mutation: each click = +1 to chosen skill, `up_skills -= 1`, persist always. UI uses `hx-confirm` for native browser confirm (one-skill-per-click, no undo, matches CLI).
9. **Gym training (0.2.1e + 0.2.1g).** `skill_training_check_done(state)` is called in `_dashboard_context` (alongside `work_check_done`) — auto-finalizes finished trainings. Start endpoint `POST /web/gym/start` (Form) / `POST /api/gym/start` (JSON `GymStartRequest`) validate via `_validate_and_apply_training(state, skill_name)` — pre-flight resource check, then existing CLI helper `Skill_Training.start_skill_training()` (try_spend + actions.start_training + decrease_durability), then persist. Pre-computed list of skills via `_build_gym_skills(state)` returns `{cost, can_afford, missing}` for the UI; insufficient-resource buttons render disabled with explicit "Не хватает: 🏃 N" line. After 0.2.1g (4.48.4.1) all skills work uniformly — `energy_max_skill` no longer special-cased. Skill count grew over time: 8 (0.2.1g) → 9 (`banking_interest_rate`, 0.2.2 / 4.49.1.0) → 11 (+ `loan_capacity` + `loan_interest_reduction`, 0.2.2 / 4.49.2.0) → 12 (+ `inspiration`, 0.2.3 / 4.27) → 13 (+ `money_saving`, 0.2.3a / 4.20) → 14 (+ `earnings_boost`, 0.2.4a / 4.23) → 15 (+ `backpack_skill`, 0.2.4b / 4.50.0) → 16 (+ `trader`, 0.2.4h / 4.28 — money trilogy completed). Порядок entries в `_GYM_SKILL_DISPLAY` определяет порядок рендера в web и CLI — money-related навыки сгруппированы вместе: `money_saving` (поз. 9), `earnings_boost` (поз. 10), `trader` (поз. 11 — третья нога трилогии), далее банк-навыки (`banking_interest_rate` / `loan_capacity` / `loan_interest_reduction` на 12-14), `inspiration` на 15, `backpack_skill` — на 16 (особняком: единственный non-bonus skill, расширяющий capacity рюкзака на +1 слот за уровень).
10. **Energy max as derived value (0.2.1g).** `state.energy_max` is no longer read directly in game logic. The canonical source is `bonus.compute_energy_max(state) = 50 + gym.energy_max_skill + equipment_energy_max_bonus + steps.daily_bonus + char_level.skill_energy_max`. The dataclass field `state.energy_max` is kept only for save-format backward compat (CSV/Sheets column), updated at load via `compute_energy_max`. Use `compute_energy_max(state)` for any cap check / display / clamp. The off-by-one reverse-calc `_energy_max_skill_level` was removed. Gym skill key was renamed `'energy_max'` → `'energy_max_skill'` to match the dataclass field name (consistent with `speed_skill`, `luck_skill`); legacy save migration in `state.from_dict` auto-converts old key.

Known follow-up: double-claim race on stale Sheets state — see TASKS.md `4.48.5.1`.

### Mutation helpers: `actions.py`

Non-trivial mutations go through `actions.py` to keep invariants in one place:

- `try_spend(state, steps, energy, money) → bool` — atomic check & deduct. Returns False without mutation if any resource is insufficient.
- `start_work(state, work_type, salary, hours, start, end)` — populate `state.work` for a session.
- `start_training(state, skill_name, time_end, timestamp)` — populate `state.training`.
- `start_adventure(state, name, start_ts, end_ts)` — populate `state.adventure`.

### Gameplay module map

- `locations.py` — dispatch layer between `game.py` and each location module; `icon_loc(state)` maps `state.loc` to an emoji.
- `gym.py`, `work.py`, `adventure.py`, `shop.py`, `inventory.py`, `equipment.py`, `bank.py` — one per location/menu. `work_check_done(state)` and `skill_training_check_done(state)` are polled from the main loop to finalize timer-based activities; if you add another timed action, follow the same "check on every tick" pattern.
- `bank.py` (since 0.2.2 / task 4.49) — депозиты + кредиты. **Capitalize-on-change**: `accrue_deposit(state)` / `accrue_loan(state)` капитализируют накопленные проценты по ТЕКУЩЕЙ ставке за `now - last_interest_ts` перед любым событием, меняющим тело или ставку. Триггеры: top-up / withdraw / take_loan / repay / апгрейд `banking_interest_rate` / апгрейд `loan_interest_reduction` (хук в `gym.skill_training_check_done` через lazy import). Ставки recompute (не lock-on-open): прокачал скилл — старый депозит / кредит сразу идёт по новой ставке (compound на событиях, exploit «качнуть → собрать ретроактивно» закрыт через accrue ПЕРЕД инкрементом). Скиллы-gate'ы: `banking_interest_rate >= 1` гейтит депозит (`can_open_deposit` + `can_withdraw`), `loan_capacity >= 1` гейтит кредит (`can_take_loan`); `loan_interest_reduction` снижает ставку с дефолтных 100% годовых. Display: 2 знака в Bank-меню, `:,.0f` в остальных меню; `state.money: float` (а не int) чтобы копейки текли между кошельком и депозитом без потерь.
- `drop.py` — item drop logic for Adventure. Grades are `c/b/a/s/s+`, weighted by `drop_percent_*` constants. `current_luck(state)` is computed at the moment of the drop (no module-level pinning). Since 0.2.4f (4.29-replacement) функция `compute_grade_probabilities(adv_name, state) -> dict[str, float]` возвращает аналитические вероятности каждого грейда (включая 'nothing') с учётом текущего luck — используется в Adventure menu для отображения %; формула: P(grade_i) = P(gate) × Σ_{r=1..min(T_i, N)} (1/N) × ((N-r)/N)^{n-1}, где N=100-luck. MC parity tests в `tests/test_drop.py` (10k×7 итераций, ~200ms) защищают от рассинхрона аналитики и реальной `one_item_random_grade`. Since 0.2.4g (balance fix) S+ threshold перестал быть глобальным — есть per-adventure overrides: `drop_percent_item_s_=15` для walk_20k (рядовой бонус среди 3 тиров), `drop_percent_item_s_walk_25k=20` (мягкий буст), `drop_percent_item_s_walk_30k=35` (endgame walk — чёткое позиционирование «специально за S+»). До 0.2.4g walk_30k был ловушкой для игрока (всего +1.41% S+ vs walk_25k за +20% цены), теперь walk_30k = ~2× преимущество по S+. Since 0.2.4c (task 4.50.1) `Drop_Item.item_collect` имеет три ветки на момент дропа: (1) `not bonus.inventory_full(state)` → append + log_event('drop'); (2) full + `state.pending_drop is None` → находка уходит в `state.pending_drop` (Optional[dict]) + одноразовое info-сообщение «🎒 Инвентарь полон. Находка ждёт решения...» + log_event('drop_pending'); (3) full + pending уже занят → forced sale (`state.money += new.price`, старый pending не трогается) + log_event('drop_force_sold'). Pending — это **один** слот на хранение, не очередь (D-iii: при коллизии новый авто-продаётся). Resolve в CLI через `inventory.inventory_menu` — prompt в начале меню при `pending != None` (3 опции: продать предмет №N + положить pending, продать pending за base price, skip — pending остаётся). Auto-collect при освобождении места (продажа / прокачка `backpack_skill` / снятие экипировки) — через `bonus.auto_collect_pending_drop(state)`, вызывается на каждом тике CLI main loop'а в `game.py` И на каждом рендере `_dashboard_context` в web (since 0.2.4d / 4.50.2) с `persist_state_to_cloud()` при transition. Web UI: pending-баннер вверху dashboard'а (3 опции: sell_new / skip как кнопки в баннере, sell_existing — inline-кнопка на каждой строке инвентаря с hidden `index` атрибутом), inventory `<details>` авто-открывается при active pending. Endpoints: `/web/drop/sell_existing` / `/web/drop/sell_new` / `/web/drop/skip` (Form, fragment swap) + `/api/drop/sell_existing` / `/api/drop/sell_new` (JSON). Persist в Sheets через round-trip flat-key 'pending_drop' (None для legacy сейвов до 0.2.4c).
- `level.py` (`CharLevel`), `characteristics.py` (`save_characteristic`), `bonus.py`, `equipment_bonus.py`, `skill_bonus.py` — stat/bonus math. All take `state` explicitly. Since 0.2.3 (task 4.27 — «Обучение» / Inspiration skill) `CharLevel.calculate_level_from_total_used_steps` и `progress_to_next_level` используют helper `_effective_xp() = state.steps.total_used + int(state.steps.xp_bonus)` вместо raw `total_used`. `xp_bonus: float` — accumulator forward-only multiplier'а: на каждый успешный `actions.try_spend(steps=N)` добавляется `N * gym.inspiration / 100.0` (если skill > 0). Property `total_used_steps` остаётся raw (отображение «Total used» в status_bar показывает реальные шаги, без bonus). При `inspiration=0` (default / старые сейвы) `xp_bonus` не растёт → поведение неизменно. Since 0.2.3a (task 4.20 — «Экономия денег» / Money Saving skill) `bonus.apply_money_saving(cost, state) -> float = round(max(0.0, cost * (1 - gym.money_saving / 100)), 2)` оборачивает `cost['money']` в Gym training (4 точки в `gym.py`) и Shop purchases (4 точки в `shop.py`); web `_build_gym_skills` + `_validate_and_apply_training` тоже учитывают скидку. Линейная формула, при skill=100 → cost=0, при skill>100 → clamp до 0. `actions.try_spend(money: float)` — сигнатура поднята с int. НЕ применяется к Bank operations / Work salary / Adventure rewards. Since 0.2.4a (task 4.23 — «Бонус к зарплате» / Earnings Boost skill) симметричный helper `bonus.apply_earnings_boost(salary, state) -> float = round(salary * (1 + gym.earnings_boost / 100), 2)` обёрнут в **5 display точек CLI work.py** (ask_hours / add_working_hours / check_requirements preview / + final credit в work_check_done) и **2 web точках** (`_build_hour_options` / `_build_work_vacancies`). `state.work.salary` остаётся **базовой** — bonus применяется на лету (recompute model: прокачал во время смены → next render preview обновится, при завершении применяется текущий уровень). Линейная без cap, на skill=100 удвоение дохода. НЕ применяется к продаже предметов / Bank / Adventure. `_dashboard_context` pre-computes `work_effective_salary` / `work_effective_total` для шаблона Active Sessions. Since 0.2.4h (task 4.28 — «Торговец» / Trader skill) helper `bonus.apply_trader(price, state) -> float = round(price * (1 + gym.trader / 100), 2)` — third leg of экономической трилогии (4.20 money_saving / 4.23 earnings_boost / 4.28 trader). Применяется во **всех 4 точках продажи** через единый helper: `inventory._sell_item_at_index` (обычная продажа), `inventory._resolve_pending_drop_sell_existing` (продажа existing item в pending-drop resolve), `inventory._resolve_pending_drop_sell_new` (продажа самой находки), `drop.Drop_Item.item_collect` forced-sale ветка (при full inventory + pending occupied). Без cap, на skill=100 — удвоение. Refund signatures поднялись `int` → `float` (round до 2 знаков) для consistency с другими money-helper'ами. Since 0.2.4b (task 4.50.0 — «Размер инвентаря» / Backpack Skill) `bonus.BASE_BACKPACK_CAPACITY = 10` + helper'ы `backpack_capacity(state) = BASE + state.gym.backpack_skill` и `inventory_full(state) = len(state.inventory) >= cap` (`>=`, а не `==` — backwards-compat: legacy сейвы с переполненным инвентарём не могут добавлять новые предметы пока не освободится слот). Capacity-check встроен в `shop._buy_item` и `equipment._unequip` (возвращают False/None при full), UI пред-проверяет `inventory_full` чтобы напечатать корректное сообщение «Рюкзак полон (N/cap). Освободи слот...». `_equip_from_inventory` (swap) — net-zero, проверку пропускает. Display: CLI `inventory_menu` / `sold_item` / `Equipment.inventory_view` показывают `(N/cap)`, web `_status_fragment.html` — `🎒 Инвентарь (N/cap)` через context-key `inventory_capacity`. Since 0.2.4c (task 4.50.1) `bonus.auto_collect_pending_drop(state) -> Optional[dict]` — pure helper для случая B (cap расширилась пока pending активен): если `state.pending_drop != None and not inventory_full(state)` → append в инвентарь + clear pending + log_event('drop_auto_collected') + return item; иначе None. Идемпотентен. Вызывается в `game.py` main loop на каждом тике (между `skill_training_check_done` и `status_bar`) и печатает one-shot уведомление «🎁 Освободилось место в рюкзаке. Находка ... добавлена в инвентарь» при transition. Since 0.2.3b (task 4.20.1) **все** money / cost / wallet displays унифицированы на `:,.2f` через `functions_02.format_money(amount, decimals=2)` — единая точка форматирования (registered as Jinja global для web). Wallet теперь показывает `2,038.00 $` вместо `2,038 $` — копейки видны везде где могут появиться (после Bank operations, money_saving discount, и т.д.).
- `adventure_data.py` — static data table for adventures. Adventure `__init__` copies entries before applying `move_optimization` so the table itself is never mutated. Since 0.2.4f (4.29-replacement) поле `drops: [(grade, threshold), ...]` — упорядоченный список тиеров для каждого приключения, импортирует `drop_percent_item_*` константы из drop.py. Единый источник правды для `drop.py:one_item_random_grade()` и `compute_grade_probabilities()`. Мёртвые поля `drop_items` (устаревшая `{ring, necklace}` — реально drop.py:item_type сэмплит 5 типов независимо от hard'а) и `drop_grade` (set без порогов) удалены.
- `skill_training_data.py` (since 0.2.3e / task 1.3.2) — static lookup-таблица стоимости прокачки Gym-навыков (1..30 уровни) + helper'ы `get_skill_training(level)` (экстраполяция формулой для level > 30) и `get_energy_training_data(level)` (wrapper для Daily Bonus). Pure data + pure helpers, без state-зависимостей. Импортёры: `gym.py`, `web/main.py`. Раньше жил в `characteristics.py:165-390`, вынесен по аналогии с `adventure_data.py` — разделение lookup-данных и state-логики.
- `functions.py` / `functions_02.py` — cross-cutting helpers (`status_bar`, `energy_time_charge`, `char_info`, date/time helpers). `functions_02.format_timedelta(td)` (since 0.2.1i) is the canonical countdown formatter for work / training / adventure timers — produces `Yг Mмес Wнед Dд H:MM:SS`. The JS `formatRemaining(seconds)` in `web/templates/dashboard.html` mirrors it constant-for-constant (year=365d, month=30d, week=7d) so CLI and web show identical strings for the same state. Existing `functions_02.time(x)` (minutes → "H час M мин." with colorama) is a separate display helper for activity costs in gym/work/adventure cost menus and is unchanged.
- `settings.py` — `debug_mode` flag consumed by many modules for verbose logging.
- `version.py` (since 0.2.4 / task 4.6) — единая точка правды для `VERSION` constant. Импортируется в `game.py` (CLI startup print), `web/main.py` (FastAPI app version), `history.py` (поле `game_version` в каждом event'е). Раньше дублировалась в game.py и web/main.py — каждый bump требовал двух правок. Теперь bump в одном месте.
- `history.py` (since 0.2.4 / task 4.6) — append-only лог значимых игровых событий. Единая точка `log_event(event_type, **payload) -> None`. Dual-write: local `history.jsonl` (sync, fail-resistant — печатает warning на I/O error) + Sheets `history` лист (best-effort через `HistoryLogRepo.append`, fail-silent). 22 типа событий в 15 mutation-сайтах. См. также Persistence layers выше.

### Type hints

Since 0.2.1j-r (task 5.1, 05.05.2026) the codebase has ~100% type hint coverage across all modules — `state.py`, `actions.py`, `bonus.py`, `equipment_bonus.py`, `skill_bonus.py`, `level.py`, `characteristics.py`, `gym.py`, `work.py`, `adventure.py`, `inventory.py`, `equipment.py`, `shop.py`, `functions.py`, `functions_02.py`. Since 0.2.1u (task 5.6, 02.05.2026) **mypy подключён** — конфиг в `mypy.ini` с пермиссивным baseline (`disallow_untyped_defs=False`, `strict_optional=False`, `ignore_missing_imports=True`), запуск через `mypy .`, на момент подключения 0 ошибок. CI step пока не настроен (нет CI вообще), запускается локально перед коммитами. New code should add hints by default. Pattern: parameters `state: GameState`, return types explicit (`-> bool`, `-> None`, `-> tuple[X, Y]`). `Optional[X]` from `typing` for nullable params; `dict[str, int]` / `list[dict]` parameterized where structure is known. Item dicts (`state.inventory`, `state.equipment.<slot>`) typed as plain `dict` — TypedDict postponed to task 1.6 (Items as dataclass).

Один из побочных эффектов подключения mypy — обнаружен и исправлен реальный runtime-баг в `state.AdventureSession.end_ts` (5.6.1): тип был `Optional[datetime]`, а на запись/чтение всегда использовался Unix timestamp (`float`). `_deser_datetime(float)` возвращал `None`, поэтому `adventure_end_timestamp` терялся при каждом load — игрок терял прогресс активного приключения после reload/restart. Тип переведён на `Optional[float]`, `from_dict` читает значение напрямую без `_deser_datetime`. Урок: type-mismatch не всегда косметика — иногда это потерянное состояние.

### Tests

`tests/` contains pytest suites for each module. Pure logic helpers (`_sort_inventory`, `_buy_item`, `_equip_from_inventory`, `try_spend`, formula functions, `_state_dict_to_rows`, `_rows_to_state_dict`) are tested directly with hand-built `GameState` instances. UI methods that use `input()`/`print()` are tested via `capsys` and `monkeypatch.setattr('builtins.input', ...)`. Sheets repos (`GameStateRepo`, `StepsLogRepo`) are tested with mocked gspread (`unittest.mock.MagicMock`) — no real network. Tests do not depend on Google Sheets at all after task 1.2 (`import characteristics` is a pure operation), so the full suite runs in ~1 sec.

### Credentials and ignored files

`.gitignore` excludes the `credentials/` directory (which must contain `2walks_service_account.json` for Sheets to work). When running locally for the first time, expect Sheets calls to fail until that file is provided; loaders fall back to CSV automatically (`characteristics.py:92`).
