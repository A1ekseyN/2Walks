# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project context

2Walks is a step-counter RPG written in Python. Real-world steps (entered manually via the `+` command) fuel in-game actions: training at the Gym, Work shifts, Adventures with item drops, deposits / loans at the Bank, etc. Comments and UI text are primarily in Russian. The project targets desktop (Mac); an Android/Kivy build existed historically but was removed — see `git log` before 2026-04-24 if you need to resurrect any of it. A Google Fit auto-sync existed historically but was removed on 2026-04-27 (task 4.16); iPhone Shortcut pipeline (4.13) is currently отложено — entry will be via CLI / Web / API (task 4.48.2 `POST /api/steps`).

**Primary interface:** CLI (`game.py`). **Secondary (deployed 18.05.2026):** Web interface via FastAPI backend on home Ubuntu server (task 4.48.0.1 done; hostname `aleksey-H61M-DS2H`, systemd unit `2walks.service`, доступ из домашней сети по mDNS `http://aleksey-H61M-DS2H.local:8008` — **рекомендуется**, устойчиво к смене IP — или по IP `http://192.168.1.155:8008`). **NB: IP сервера в локалке меняется** при перезагрузке роутера (был `192.168.0.155`, стал `192.168.1.155` 30.06.2026); mDNS-имя `aleksey-H61M-DS2H.local` его переживает. Если что-то перестало резолвиться — узнать новый IP: `ping aleksey-H61M-DS2H.local`. CLI остаётся primary path для дебага / экспериментов, web — для повседневной игры с iPhone; concurrent usage защищён через 4.54 optimistic concurrency. **Web feature parity почти полная (с 0.2.4w / 19.05.2026; Forge добавлен 0.2.6c / 28.05.2026)** — Work / Gym / Adventure / Inventory / Equipment / Loadout (optimizer + presets) / Bank / Кузница (Repair + Crafting) доступны через web. Остался только Shop (4.48.7, blocked by 4.7 CLI Shop reform). Single source of truth для CLI и web — Google Sheets. Updates на сервере: `make deploy` (обёртка над `ssh -t aleksey@aleksey-H61M-DS2H.local "cd ~/2Walks && git pull && .venv/bin/pip install -r requirements.txt && sudo systemctl restart 2walks"` — `-t` обязателен для sudo-пароля — см. `Makefile`; `HOST` по умолчанию mDNS-имя, переопределяется `make deploy HOST=aleksey@192.168.1.155`, плюс `make status` / `make logs`). SSH интерактивный — запускать вручную. См. `docs/local_setup.md` раздел «Production: web 24/7».

## Entry points

A single runnable root:

- `game.py` — pure CLI REPL (`location_selection()` loop). Also re-exported as a function `game()` for programmatic use.

## Common commands

```bash
# Run the game (CLI)
python game.py

# Run the web interface (FastAPI, dev mode). Requires venv active or call .venv/bin/uvicorn directly.
source .venv/bin/activate && uvicorn web.main:app --reload --host 127.0.0.1 --port 8008

# Smoke-проверка БЕЗ загрязнения прода (4.54.0.3): DRY_RUN=1 → все persistence
# write-paths (Sheets game_state/steps_log/history + state.json/history.jsonl) no-op,
# reads работают. Защищает и web-смок, и `python -c`-смок. Печатает warning на старте.
DRY_RUN=1 uvicorn web.main:app --reload --port 8008   # или DRY_RUN=1 python -c "..."

# Show current Sheets game_state contents (debugging)
python google_sheets_db.py

# One-time migration of Google Sheets layout (renames Sheet1 → game_state, creates steps_log)
python migrate_sheets.py

# Drop-rate Monte-Carlo simulation (5 luck × 7 difficulty × 10k iterations on real Drop_Item)
python drop_test_montecarlo.py

# Static type check (mypy with permissive baseline — see mypy.ini)
mypy .
```

Dev-only deps (mypy и т.д.) живут в `requirements-dev.txt` — установка `pip install -r requirements-dev.txt`. Основные runtime-зависимости — `requirements.txt`.

CLI и web — отдельные процессы, у каждого свой `game.state`. **Concurrent usage защищён через optimistic concurrency** (4.54, 0.2.4p): каждый save проходит через `GameStateRepo.save_safe()`, который сравнивает `state.last_modified` с актуальным `last_modified` в Sheets — при mismatch (другой процесс успел записать первым) save возвращает `"STALE"` без записи. CLI на STALE показывает интерактивный prompt (`r` Reload / `f` Force с двойным подтверждением / `c` Cancel) с подробным diff'ом изменений; web — автоматический toast «⚠️ Состояние обновлено извне: ...» + `window.location.reload()` через 2 сек. Все STALE-события логируются в `history.jsonl` (`type='sync_conflict'`, source `cli`/`web`). Финализаторы (`work_check_done`, `skill_training_check_done`, `auto_collect_pending_drop`, `save_game_date_last_enter`) толерантны к STALE — мутируют state в RAM, печатают warning при STALE save, на следующий user-save игрок получит prompt и через Reload подтянет всё. Старый паттерн «закрывать CLI без сохранения» больше не нужен — данные защищены, но F5 в web после CLI-save и `r` в CLI после web-save рекомендуются для синхронизации snapshot'а. Diff helpers и end-to-end сценарии — `sync_diff.py` + `tests/test_sync_e2e.py`.

Подробная шпаргалка по локальному запуску (включая доступ с iPhone через домашнюю Wi-Fi) — [`docs/local_setup.md`](docs/local_setup.md).

Pytest is the test framework (config in `pytest.ini`, tests in `tests/`). Run all tests with `.venv/bin/pytest tests/`. There is no linter or CI configured. `drop_test_montecarlo.py` is a standalone Monte-Carlo drop simulator — uses the **real** `Drop_Item.one_item_random_grade(hard, state)` from `drop.py` (no fork-copy after 3.2.2 / 0.2.1f), so its measurements correspond to the actual game balance and are usable for tuning `drop_percent_*` constants.

## Architecture

### Game state: `GameState` (state.py)

The single source of game state is a `GameState` dataclass in `state.py` with nested subclasses (`StepsState`, `CharLevel`, `GymSkills`, `TrainingSession`, `WorkSession`, `AdventureSession`, `Equipment`, `BankState`). The live instance is held by a container `game = _GameContainer()` in `characteristics.py` and accessed as `game.state` — populated by `init_game_state()` (task 1.2). Most gameplay modules take `state: GameState` as an explicit parameter — there is no implicit global. Mutate `state.<sub>.<field>` to communicate between systems.

A historical legacy dict `char_characteristic` and proxy class were removed in version 0.2.0 (task 1.1). If you encounter that name in old commits or external docs, it referred to today's `game.state`.

**Initialization is now lazy** (task 1.2, version 0.2.0c). Importing `characteristics` does NOT touch Google Sheets — only the call to `init_game_state()` does. CLI calls it from `__main__` before `play()`; FastAPI (task 4.48) will call it from a startup hook. Tests construct their own `GameState` and never need init at all, so the test suite runs in ~1 sec.

### Persistence layers (three of them)

Saves are written to **both** on save; loads prefer Google Sheets with CSV fallback:

1. **`state.json`** — primary локальный фолбэк (since 1.4.3 / 0.2.5, 20.05.2026). Полный `state.to_dict()` через `json.dump` с indent=2, datetime сериализуется через `_json_default` в legacy strftime формат (`_DATETIME_FMT = '%Y-%m-%d %H:%M:%S.%f'`). Save/load живёт в **`persistence.py`**: `save_characteristic() -> Literal["OK","STALE"]` пишет state.json после успешного Sheets save_safe; `load_state_json()` читает + конвертирует `_DATETIME_KEYS` обратно через strptime. **Legacy CSV fallback** — `characteristic.csv` (плоский Key/Value, флат) сохранён в `load_characteristic()` для backwards-compat с pre-0.2.5 сейвами: на первом load orchestrator `_load_local_fallback()` пробует `state.json` первым, при отсутствии файла читает CSV. Первый успешный save после deploy перезаписывает в state.json — auto-migration. CSV reader + старый `characteristic.txt` (write-only zombie, удалён в 0.2.3c / 1.4.1) удалятся в 1.4.3.1 Legacy cleanup через 1-2 недели после bake-test'а.
2. Google Sheets — three specialized worksheets in one spreadsheet (task 4.14 + 4.6, version 0.2.0d / 0.2.4):
    - `game_state` — full state snapshot. **С 1.4.3 (0.2.5) — JSON-blob layout** (single row, 2 cells): `A1` = `last_modified` (float, для fast `load_meta`), `B1` = JSON-blob всего state через `state.to_dict()` + `json.dumps`. Datetime serializes через `_json_blob_default` в legacy strftime формат. **Critical (4.48.5.4 / 0.2.5d):** `load_meta` читает A1 через `ws.acell('A1', value_render_option=ValueRenderOption.unformatted).value` — обязательно UNFORMATTED, потому что ячейка A1 имеет дефолтное Sheets-форматирование "Number" без decimals → FORMATTED display округляет float до integer (`1779300150.71383` → `'1779300151'`) → разница 0.286 сек > 0.01 epsilon → false-positive STALE на каждом save. `load()` той же проблемы не имеет — читает B1 (full precision JSON через `_blob_rows_to_state_dict`). Раньше — Key/Value layout (~80 rows, header + per-key); auto-detect в `GameStateRepo.load` распознаёт legacy формат по first row `['Key', 'Value']` и парсит старым `_rows_to_state_dict`. Первый save переписывает в JSON-blob (auto-migration). Перед deploy на сервер запускается `scripts/backup_sheets_before_migration.py` (one-shot экспорт в `backup_before_1.4.3_*.json`). Renamed from legacy `Sheet1` (4.14).
    - `steps_log` — append-only log of step measurements (`ts | user_id | steps | source`). `ts` is Unix timestamp (`float`); `source` is `'manual'` (CLI), `'web'` (web form / `POST /api/steps`), `'auto'` (future iPhone Shortcut, currently отложено). Used by max-merge (`apply_steps_log_max_merge`, task 4.15) — applied on every load (CLI start + web reload) so any input channel reflects on next start, even if the `game_state` snapshot is stale.
    - `history` — append-only лог значимых игровых событий (task 4.6, since 0.2.4). Колонки: `ts | datetime | user_id | game_version | event_type | payload_json`. Dual-write: local `history.jsonl` (primary, sync, fail-resistant) + Sheets `history` (best-effort, fail-silent через `HistoryLogRepo.append`). Sheets копия для будущих dashboard'ов (4.10) и multi-device access. На каждое значимое событие (work_done, skill_upgraded, deposit, drop, level_up, item_bought / sold, etc. — 24 типа после 0.2.4c, +5 drop-related: drop_pending, drop_force_sold, drop_auto_collected, drop_resolved_sell_existing, drop_resolved_sell_new — задача 4.50.1) вызывается `history.log_event(type, **payload)`. Schema event'а с версионированием: `{v, ts, date, time, user_id, game_version, type, payload}` — `v` для будущей миграции, `game_version` для отладки (понять какие фичи были на момент события). Read API: `HistoryLogRepo.since(ts) -> list[dict]` (since 0.2.5e / task 4.2) — full read через `get_values(value_render_option=UNFORMATTED)` + Python-filter `ts >= since`; UNFORMATTED критично для precision (грабли из 4.48.5.4); silent-fail на network. Используется для «пока тебя не было» report (см. `report.py` ниже). Pruning / CLI viewer / re-sync after Sheets recovery — отложенные follow-up подзадачи (4.6.1 / 4.6.2 / 4.6.3). Pytest fixture `tests/conftest.py` с autouse отключает `log_event` для всех тестов кроме `test_history.py` — иначе тесты загрязняют реальные local file и Sheets.

Web dashboard refresh model (after 0.2.0j; **async-reload since 0.2.6 / 4.48.5.5**): no automatic HTMX polling. **`GET /` рендерит shell мгновенно из in-memory state** через `_dashboard_context(defer_sync=True)` — БЕЗ Sheets / rollover / persist (read-only). Затем страница сама дёргает **`GET /reload`** (HTMX `hx-trigger="load"` на невидимом `<div>`), который делает тяжёлый sync (`try_reload_state()` = Sheets load + max-merge + day rollover detect/persist + snapshot) и swap'ает свежий `_status_fragment.html` в `#status-bar`. До 4.48.5.5 `GET /` синхронно блокировался на `try_reload_state()` (2–5 Sheets round-trip'ов, особенно на смене дня), и страница «зависала» с пустым экраном + нативным браузерным индикатором — overlay-спиннер жил на выгружаемой странице и не анимировался. Теперь overlay (`#save-overlay`) показывается мгновенно при первом рендере (`body.reloading`, серверный текст: `🌅 Начался новый день…` если `pending_rollover` = `date_last_enter != today`, иначе `Синхронизирую данные…` без эмодзи) и снимается в `htmx:afterSwap`. `defer_sync` гейтит non-идемпотентный rollover (`new_day` лог) + finalizers + persist — их делает `/reload` через 1–2 сек. **Cloud sync badge** («Cloud sync failed») переехал из shell во `_status_fragment.html` — при сбое Sheets в `/reload` приходит вместе со swap'ом. Numbers также обновляются after form submit (`POST /web/steps`, `POST /web/work/*` etc. — endpoints return updated fragment, HTMX swaps `#status-bar`). Active session timers (training / work / adventure) tick every second via JS without server roundtrips. **Session detail modal (4.48.10 / 0.2.6):** клик по блоку активной сессии в `#active-sessions` открывает Pico `<dialog>` (`data-open-session-modal` → JS `showModal()`, backdrop/кнопка закрывают) с деталями — Прошло (live, `[data-elapsed-start-ts]` через `formatRemaining`) / Осталось / Длительность (`format_minutes`) / Прогресс % + синий `<progress>` бар / Начато·Завершится (`[data-abs-ts]` → `toLocaleString`); Work — ставка/час + итог + часы, Gym — навык current→target, Adventure — пройдено раз + drop-вероятности (из `adventure_view`). Карточка модалки — `box-shadow` (`#active-sessions dialog article`) для отделения от фона. Client-side, серверная логика не менялась (данные уже в context). The `GET /status` endpoint remains in code for future use (Refresh button — 4.54.0.1, action endpoints — 4.48.3+).

Dashboard layout (compact / mobile-first): the always-visible Stats area shows only current numbers (Steps, Energy, Money, Level + progress); клик по **всему блоку** Stats открывает inline-форму ввода шагов (4.48.2.2 / 0.2.6 — `data-toggle-steps` на `<article>`, клики внутри формы исключены в JS). Section order (21.05.2026 — Work поменян местами с Adventure, типичный flow — игрок чаще запускает работу; Кузница добавлена после Инвентаря в 0.2.6c): Stats → ⏱ Активные сессии (only if any active) → 🏭 Работа → 🗺 Приключение → 🏋 Спортзал → 🏦 Банк → 📈 Бонусы → 🧥 Экипировка → 🎒 Инвентарь → 🔨 Кузница → 🏆 Triumphs. Все sections кроме Stats / Active sessions wrapped в native HTML5 `<details>` и collapsed by default — click summary to expand:
- **🏭 Работа** — summary shows current shift inline (`watchman · 8 $ (4 ч)`) when active, or "выбери вакансию" otherwise. Inside: 4-vacancy menu with hour buttons (`1ч..Nч`, cap 8) when not working, or `+Nч` add-hours form when working. Collapsed in BOTH states (working and not) — Stats stays the primary view.
- **📈 Бонусы** — Steps breakdown (stamina · equipment · daily · level + total/percent), Energy breakdown, Total used.
- **🧥 Экипировка** — summary shows worn count `(N/8)` (8-й слот «Спина»/рюкзак — 4.51) + non-zero equipment bonuses inline (zero values filtered); inside — slot-by-slot list.
- **🎒 Инвентарь** — summary shows item count `(N/cap)`; inside — предметы **сгруппированы по типу** (since 0.2.7d): каждый item_type в своём вложенном `<details>` «emoji Название (count)» (🪖 Шлемы / 📿 Ожерелья / 👕 Футболки / 💍 Кольца / 👟 Обувь / 🎒 Рюкзаки), порядок групп канонический (как слоты), пустые типы скрыты, неизвестные → fallback-группа «📦» в конце. Группы свёрнуты по умолчанию (раскрываются все при active pending-drop находке). Сортировка (dropdown) сортирует **внутри** групп. `_build_inventory_view` отдаёт `groups` (`{type, emoji, label, count, items}`) рядом с плоским `items`; константа `_ITEM_TYPE_GROUP_LABELS`; CSS `.inv-group`. Cap = `bonus.backpack_capacity(state)` = `BASE_BACKPACK_CAPACITY (10)` + `state.gym.backpack_skill` + `backpack_slots_bonus` (надетый рюкзак +2..10 по грейду — 4.51) (см. 4.50.0 / 4.51). **Pending-drop UX (since 0.2.7e — 4.50.2 финальный дизайн):** при active `state.pending_drop` строки инвентаря показывают **обычные** кнопки (🧥 Надеть / 💰 Продать) — спец-кнопка «Продать + взять находку» убрана; продажа/надевание любого предмета освобождает слот, и `auto_collect_pending_drop` (на каждом рендере `_dashboard_context`) сам кладёт находку в инвентарь. Над всеми секциями рендерится pending-баннер «🎁 Найдена находка» с одной кнопкой «💰 Продать находку» (`POST /web/drop/sell_new`); цена в баннере — `sell_price` (с учётом навыка «Торговец» через `apply_trader`, как строки инвентаря). Баннер **НЕ закрывается вручную** — висит, пока находка не разрешена (продана ИЛИ авто-подобрана). `_build_pending_drop_view` отдаёт `active`/`banner` (оба True при наличии pending; механизм «Отложить» удалён) + `item` (с `price` сырой и `sell_price` trader-adjusted); пока pending активна, `<details>` инвентаря auto-expand'ит (`open`). Эндпоинты `POST /web/drop/sell_existing` + `POST /api/drop/{sell_existing,sell_new}` оставлены как API-зеркала, но из web-UI не вызываются (`/web/drop/skip` удалён). Когда авто-подбор срабатывает — игрок видит баннер «🎁 Находка подобрана» в session-events (`push_session_event('drop_auto_collected')`, висит до dismiss).

Collapsed content stays in DOM (accessibility / search-friendly), only visually hidden by Pico.css default styles. After HTMX swap (any `POST /web/*` endpoint) sections close — by design (no state persistence between renders).

**Static files (since 0.2.4l):** `web/static/` mounted at `/static` через `app.mount("/static", StaticFiles(directory=...))`. Сейчас содержит только `apple-touch-icon.png` (180×180 PNG с эмодзи 🏃 для iPhone home-screen icon). Browser tab favicon — SVG inline data-URI в `<head>` `dashboard.html` (`<link rel="icon" href="data:image/svg+xml,<svg>...<text>🏃</text></svg>">`), без файла. Регенерация PNG — через `scripts/generate_favicon.py` (dev-only, требует `pip install 'Pillow>=10.0'` — НЕ в requirements-dev.txt чтобы не таскать runtime-зависимость). Готовая инфраструктура для будущих ассетов (4.56 фоны локаций).

   Access via `google_sheets_db.GameStateRepo` (save/load) and `StepsLogRepo` (append/for_day) classes. A lazy singleton `_get_client()` keeps one authorized gspread client per process. New deployments need a one-time `python migrate_sheets.py` to rename `Sheet1` and create `steps_log`.

   **Locale-safe float serialization (since 0.2.3g):** Sheets API без `value_input_option=RAW` интерпретирует числовые значения по системной локали. На локали с запятой как десятичным разделителем (ru-RU и т.п.) float `2018.10` сохраняется как `'2018,1'`, на чтении `ast.literal_eval('2018,1')` → tuple `(2018, 1)` → crash в `from_dict: float(tuple)`. Фикс: (1) `_state_dict_to_rows` конвертирует float в строку через `repr()` ('2018.1' с точкой); (2) `GameStateRepo.save` использует `value_input_option=ValueInputOption.raw`; (3) `StepsLogRepo.append` — то же `RAW` для `ts: float`. Без этого фикса все float-поля (money, bank_*, steps_xp_bonus, timestamps) ломаются на любой локали кроме en-US. При написании новых float-полей или новых call-sites в Sheets — **обязательно использовать RAW** (через `value_input_option=ValueInputOption.raw` в `update`/`append_row`).

   Steps_log writes happen only on explicit save (`s` or `q`) — not on every `+N` input — to keep offline-mode usable (player can enter wrong steps and exit without save).

When adding new fields to `GameState`, update both `from_dict` and `to_dict` so the round-trip stays clean (the datetime special-case list in `_deser_datetime` / `json_serial` is the common trap).

### Step-count input

`state.steps.today` is set by one of three input channels:
1. **CLI**: `+N` command in main menu invokes `steps_today_manual_entry(state)` — `max(current, entered)`.
2. **Web form**: клик по блоку Stats на dashboard (4.48.2.2 — весь `<article>`, не только строка Steps) → inline-форма → `POST /web/steps` (form-data) → `_apply_new_steps()`.
3. **API**: `POST /api/steps` with JSON `{"steps": int}` — same helper, used by curl / future iPhone Shortcut.

All three write a row to the `steps_log` Sheet; web/API additionally update `state.steps.today` in memory immediately. The `apply_steps_log_max_merge()` helper (task 4.15) runs on every load (CLI start via `init_game_state`, web reload via `web.sync.try_reload_state`) and bumps `state.steps.today` to the maximum of all log entries for today — so input from any channel becomes visible on next start even if the `game_state` snapshot in Sheets is stale.

On date change (`save_game_date_last_enter(state)`), `today` and `used` reset to `0` so the new day starts fresh; the player re-enters the bracelet reading via any channel. The single source of truth for day rollover is `state.date_last_enter` (compared against `datetime.now().date()`); legacy `save.txt` was removed in 0.2.0k (task 2.1). **Rollover on startup (4.54.0.4, 0.2.6):** `init_game_state` выполняет rollover (после max-merge, до triumph-блока) для ОБЕИХ платформ и при **реальной** смене дня (непустой `old_date != today`) сразу `save_characteristic()` — иначе Sheets оставался со вчерашней датой до user-save, и другой процесс (web/cli) при load повторно детектил «New Day» (double-rollover, замечен 27.05.2026). Пустая дата (новая игра) persist НЕ триггерит. CLI `status_bar` / web `_dashboard_context` продолжают звать `save_game_date_last_enter` как defense-in-depth (same-day no-op + midnight-during-session).

### Web mutation endpoints (4.48.5+)

Web actions that change state (currently Work — start shift / add hours; later training and adventure) follow a common pattern:

1. **Validate + apply via shared helper.** Each subsystem has a `_validate_and_apply_<thing>(state, ...) -> Optional[str]` helper in `web/main.py` that does range/resource checks first, then calls the existing CLI mutation path (`Work.check_requirements()` for Work — which under the hood does `actions.try_spend` + `actions.start_work`), and finally calls `persist_state_to_cloud()` on success. Returns error text or None.
2. **Persist after every successful mutation.** `persist_state_to_cloud()` (in `web/sync.py`) runs `save_characteristic()` (CSV+JSON, guaranteed) followed by `GameStateRepo().save(state.to_dict())` (Sheets, best-effort — Sheets failure is logged but does not block the endpoint). Without this step, mutations would live only in uvicorn RAM and be lost on restart / invisible to CLI. Steps (`_apply_new_steps`) are the exception — they write to the append-only `steps_log` and rely on max-merge for resync, so they don't need a full snapshot save (except after a rollover — see below).
3. **Two endpoints per action: Form + JSON.** `POST /web/<action>` accepts form-data and returns the rendered `_status_fragment.html` for HTMX swap. `POST /api/<action>` accepts JSON via Pydantic models (e.g. `WorkStartRequest`, `WorkAddHoursRequest`) and returns JSON `{ok, work: {...}}` or `{ok: false, error}`. Both share the same validate/apply helper.
4. **Auto-finalize timer-based sessions.** Finalizers like `work_check_done(state)` (and future training / adventure analogs) are called at the top of `_dashboard_context()` so any GET / POST to web automatically closes finished sessions and credits rewards — no separate "Claim" click needed in web. CLI still calls these in its main loop tick.
5. **No mid-session vacancy switch.** Work is intentionally locked to the chosen vacancy until the shift ends (matches CLI). `add_hours` uses `state.work.work_type` from state rather than accepting it from the request.
6. **Day rollover (4.54.0.2).** `save_game_date_last_enter(state)` is called from three places: (a) `try_reload_state` after `update_from_dict` — primary trigger on F5 / pull-to-refresh; (b) `_dashboard_context` — defense-in-depth for HTMX swap chains that don't go through reload; (c) `_apply_new_steps` — covers `POST /api/steps` from iPhone Shortcut without prior GET /. After an actual date change, `persist_state_to_cloud()` is called so CLI and web see the same snapshot. Active sessions (`state.work` / `state.training` / `state.adventure`) are NOT touched on rollover — timers run through midnight as in CLI (a 23:30 → 01:30 work shift completes naturally on the next day).
7. **Energy auto-regen (0.2.1c, refactored in 0.2.4i / 4.21).** `energy_time_charge(state)` is called in `_dashboard_context` on every render — same helper CLI uses on each main loop tick. State.energy grows in uvicorn RAM at the rate `+1 / bonus.energy_regen_interval(60, state)` seconds (после 0.2.4i — ранее использовался `speed_skill_equipment_and_level_bonus`, теперь зависит только от `energy_regen_skill` + `skill_energy_regen` allocation). NO persist — energy changes too often, writing every render would burn Sheets quota for no benefit; next real mutation (work start / steps) syncs the actual snapshot. Client-side: a JS `tickEnergy()` runs every 60 seconds (matches the regen rate), reads `data-energy-stamp` / `data-energy-interval` attributes, recomputes `min(energy + floor((now - stamp) / interval), max)` on the fly. At max — number stays static (no special UI marker).
**Поломка предметов (4.61, 0.2.4x).** Quality перестал быть чисто косметикой. **Binary cliff (MVP):** `quality == 0` → broken item, **0 bonus**; `quality > 0` (даже 0.1) → полный bonus (без partial penalty). Helper `equipment_bonus._is_broken(item) -> bool` (strict 0 check, legacy items без поля quality → НЕ broken). 5 bonus-функций (`equipment_bonus` aggregator + 4 individual) пропускают broken items. Wear formula без изменений (100k шагов = -1% quality, `neatness_in_using_things` skill замедляет — теперь становится осмысленным). Auto-unequip НЕ делается — broken остаётся в слоте, игрок решает (Forge repair / Inventory sell). Migration: respect current quality (без free reset — игрок узнаёт о новой механике органично). **Visual feedback:** `low_quality_equipped_items(state, threshold=20)` returns equipped items < 20%; status_bar (CLI) показывает 2 раздельных warning'а («🔨 Сломано: N» красный + «⚠ Требует ремонта: N» жёлтый), web Stats area — janтарный banner с теми же двумя строками. Equipment menu / web section помечают broken items «🔨 СЛОМАН» префиксом + bonus отображается «+0» вместо «+N». **Binary cliff — финальный дизайн:** tiered partial bonus (задача 4.61.1) **отменён 26.05.2026** — градация 75/50% не вводится, остаётся бинарная механика broken/full.

**Web: Банк (4.48.9, 0.2.4w).** Депозиты + кредиты через web — раньше только CLI Bank location. Все pure helpers из `bank.py` (4.49) переиспользованы как есть. **Single dispatcher** `_validate_and_apply_bank_op(state, op_name, amount?)` на 7 ops (deposit / deposit_all / withdraw / withdraw_all / take_loan / repay_loan / repay_all) с explicit pre-checks в каждой ветке для понятных error messages. `_build_bank_view(state)` pre-compute: deposit/loan + preview_amount (с capitalized %), rates (current_deposit_rate_pct / current_loan_rate_pct), max_loan_amount, loan_available, 4 can_* gate flags, locked_reason для summary при полной блокировке. 14 endpoints: 7 web (Form, HTMX swap) + 7 API (JSON через single `BankAmountRequest(amount: int, gt=0)` для amount-based; `*_all` без body). **Auto-accrue в `_dashboard_context`** на каждом render: `accrue_deposit(state)` + `accrue_loan(state)` — симметрично CLI capitalize-on-change pattern. Persist НЕ делается на render (как `energy_time_charge`) — следующая mutation подтянет snapshot. UI: новая `<section id="bank">` между Gym и Бонусы, collapsible. Summary inline показывает active deposit/loan суммы или 🔒. Депозит + Кредит sub-blocks с inline формами. Critical ops с `hx-confirm`: take_loan с **rate-инфо** в тексте; withdraw_all / repay_all с суммой. Withdraw/Repay кнопки скрыты когда нет депозита/долга. Locked-state messaging — при skill=0 hint про прокачку.

**Web: Кузница (4.48.11, 0.2.6c).** Repair + Crafting через web — раньше Forge был CLI-only. Все pure helpers из `forge.py` (4.59.1 / 4.59.2) + forge-скидки (4.60) переиспользованы как есть. **С 0.2.6e (4.59.4.2) операции таймерные** — endpoints зовут `start_repair_session`/`start_craft_session`, финализирует `forge_check_done` в `_dashboard_context`; при active forge-сессии формы скрыты + сессия в «⏱ Активные сессии» (см. «Web (4.59.4.2)» в forge.py-описании). Ниже — UX мгновенной версии 0.2.6c (структура форм та же). **Идентификация предметов по HTTP** — стабильные ключи `slot:<attr>` (экипировка) / `inv:<index>` (инвентарь), резолв в живой item через `_resolve_forge_item(state, key)` (как 4.48.6 inventory; нельзя передавать object refs по HTTP). `_build_forge_view(state)`: `locked` (зеркало `locations.forge_location` gate — нет forge-навыка ≥1), `repair_items` (sorted by quality asc, с key/max_pct/can_repair + `repair_cost_per_pct`), `craft_groups` (с candidates-ключами + cost/can_afford/is_capped). **Repair UX:** number-input предзаполнен max-affordable + кнопка «Макс» (inline onclick) + «Отремонтировать»; `_validate_and_apply_repair` clamp'ит percent к max (Макс-friendly). **Crafting UX:** two-step preview (симметрично loadout) — `/web/forge/craft/preview` считает `_build_craft_preview(state, key_a, key_b)` БЕЗ мутации (результат grade/quality/bonus/price + was_equipped warning) → banner с «Скрафтить» (`/web/forge/craft`) / «Отмена» (`hx-get /status`). Каждая craft-группа в списке — 2 `<select>` (item_a default opt1, item_b default opt2) → «Подготовить крафт». Apply (`_validate_and_apply_craft`) делегирует в `craft_item` (heavy-валидация: identity / same key / s+ cap / ресурсы / overflow / auto-unequip). 5 endpoints: `/web/forge/repair` + `/web/forge/craft/preview` + `/web/forge/craft` (Form HTMX) + `/api/forge/repair` (`ForgeRepairRequest`) + `/api/forge/craft` (`ForgeCraftRequest`). STALE через `_persist_and_handle_stale`. UI: `<section id="forge">` после Инвентаря; 2 collapsible sub-`<details>` (🔧 Ремонт + 🔨 Улучшение грейда); craft-details auto-open при active `forge_craft_preview`. Context keys: `forge_view` / `forge_error` / `forge_craft_preview` (None по дефолту, preview endpoint перезатирает).

**Web: Loadout — optimizer + presets (4.63.3, 0.2.4v — зонтичная 4.63 closed).** Auto-Optimizer и Equipment Presets теперь доступны через iPhone — раньше только CLI (Equipment menu пункты 7/8). Pure helpers из `loadout.py` (4.63.1+4.63.2) переиспользованы как есть. **Двухэтапный preview UX**: preview-endpoints (`/web/loadout/preview` / `/web/preset/preview_load`) возвращают fragment с banner (diff slot-by-slot + bonus before/after + lost-item warnings для preset); apply-endpoints (`/optimize` / `/preset/load`) re-вычисляют (idempotent) + применяют. Apply re-compute защищает от stale preview (если между preview и apply state изменился). «❌ Отмена» — простой `hx-get /status` без нового endpoint'а. 6 web endpoints (preview + optimize + preset save/preview_load/load/delete) + 4 API (без preview — клиенты сами рассчитывают diff). Helpers: `_build_loadout_view(state)` (4 chars с current_bonus + saved presets с summary), `_build_optimize_preview` / `_build_preset_preview` (compute diff БЕЗ мутации), `_validate_and_apply_{optimize, save_preset, load_preset, delete_preset}`. UI: новая `<section id="loadout">` после Equipment, collapsible. Auto-Optimizer block — `<select>` с 4 chars (current bonus в options) + Preview button. Presets block — list saved с summary (slots_filled + 🏃🔋⚡🍀 bonuses) + Load/Delete buttons + always-visible save form. Preview banner условный (`loadout_preview != None` в context).

**Web: Inventory + Equipment mutations (4.48.6, 0.2.4u).** Sell items / wear / unwear через web (раньше read-only display, любая mutation требовала CLI). 6 endpoints (3 Form+JSON pairs): `/web|api/inventory/sell` (`InventorySellRequest`), `/web|api/equipment/wear` (`EquipmentWearRequest` с inventory_index + optional slot_attr), `/web|api/equipment/unwear` (`EquipmentUnwearRequest`). Helpers: `_sort_inventory_view(inv, sort_key)` → `[(orig_index, item)]` (4 опции — default/grade/price/bonus; orig_index чтобы wear/sell оперировали на исходном state.inventory несмотря на sorted display); `_build_inventory_view(state, sort_key)` (pre-computed items + sort_options + trader-applied sell_price + eligible_slots); `_build_equipment_view(state)` (slots с can_unequip flag + block_reason для tooltip). Все mutations используют существующие pure helpers `inventory._sell_item_at_index` / `equipment._equip_from_inventory` / `equipment._unequip` (с 4.50). Ring-handling: explicit 2 кнопки «На палец 1» / «На палец 2» в UI (не auto-pick, игрок явно выбирает finger_01/finger_02). Non-ring item — одна кнопка «🧥 Надеть» (slot auto-determined из `_ITEM_TYPE_TO_SLOTS`). Auto-swap при занятом слоте (как в CLI). Capacity check: если `inventory_full` → unwear disabled с tooltip (без этого `_unequip` сам бы вернул None — pre-check для понятного UX). Sort dropdown через server-side `<select onchange="this.form.submit()">` (без JS-sort чтобы сохранить orig_index связь); hidden `sort` input в mutation формах preserves выбор через HTMX swap; GET / принимает `?sort=key` query param чтобы выживало F5. **Разблокирует 4.63.3** Web для optimizer + presets.

**Web: Adventure (4.48.3, 0.2.4s — последний непокрытый игровой блок на момент задачи).** В `_status_fragment.html` секция `<section id="adventure">` между Work и Gym — 7 прогулок с прогрессивной разблокировкой (3 прохождения предыдущего тира), drop probabilities через `drop.compute_grade_probabilities` (учитывает current luck). Endpoints: `POST /web/adventure/start` (Form) + `POST /api/adventure/start` (JSON, `AdventureStartRequest`). Auto-finalize через `_finalize_adventure_with_drop_capture(state)` в `_dashboard_context` — wrapper вокруг `Adventure.adventure_check_done`, детектит transition `active=True → False` через delta inventory/pending_drop. **Since 0.2.6n (4.48.12.1) итог дропа объединён в баннер «🗺 Приключение пройдено»** (session-event `adventure_done`, см. «уведомления о завершении» в 4.48.12): финализатор извлекает находку из буфера `deferred` (`drop` / `drop_pending` / `drop_force_sold`) и кладёт статичный снапшот + `drop_ref` (object-ref на dict в инвентаре, безопасно т.к. `session_events` runtime-only) в payload события; баннер показывает `🎁 Нашли: <Type> (<grade_short>) · +<bonus> <Char> · качество <q> · <price> $` (с кнопками 🧥 Надеть / 💰 Продать, кольцо → «На палец 1/2») либо `🔍 Находок нет`. Кнопки резолвят индекс по identity (`is`) per-event — пока находка реально в инвентаре (после mutation предмет уходит → кнопки исчезают, текст «Нашли…» остаётся). Прежний отдельный зелёный баннер `adventure-drop-notification` + helper `_build_drop_notification_view` + context-ключи `drop_notification`/`drop_notification_view` удалены; `state.last_adventure_drop` (runtime-only, НЕ сериализуется как `last_loaded_snapshot`) остаётся как snapshot capture в финализаторе + STALE-rollback, очищается на любом успешном mutation (`_persist_and_handle_stale` + `_apply_new_steps`). Helpers: `_build_adventure_view(state)` pre-computes list из 7 dict'ов с locked/unlock_hint/cost/can_afford/missing/probabilities; `_validate_and_apply_adventure(state, adv_name)` checks already-active / unknown / locked / insufficient → try_spend → actions.start_adventure → decrease_durability → log_event → persist. Display polish: cost time через `format_minutes` (human-readable «1 ч. 43 мин.» вместо raw «103 мин»), grade labels через `_GRADE_LABELS` constant — полные формы «C-Grade» / «B-Grade» / ... / «S+ Grade» (с пробелом для S+ чтобы избежать двойного даша «S+-Grade»). Pattern полностью симметричен 4.48.5 Work / 4.48.4 Gym.

8. **Level-up auto-detection (0.2.1d).** `CharLevel(state).update_level()` is called in `_dashboard_context` on every render. Without this, web-only players never crossed level thresholds (CLI's `level_status_bar` was the only caller). Persist only when `state.char_level.level` actually changed — same-level renders are no-ops. Skill point allocation (`POST /web/level/allocate` / `POST /api/level/allocate`) is the partner mutation: each click = +1 to chosen skill, `up_skills -= 1`, persist always. UI uses `hx-confirm` for native browser confirm (one-skill-per-click, no undo, matches CLI).
9. **Gym training (0.2.1e + 0.2.1g).** `skill_training_check_done(state)` is called in `_dashboard_context` (alongside `work_check_done`) — auto-finalizes finished trainings. Start endpoint `POST /web/gym/start` (Form) / `POST /api/gym/start` (JSON `GymStartRequest`) validate via `_validate_and_apply_training(state, skill_name)` — pre-flight resource check, then existing CLI helper `Skill_Training.start_skill_training()` (try_spend + actions.start_training + decrease_durability), then persist. Pre-computed list of skills via `_build_gym_skills(state)` returns `{cost, can_afford, missing}` for the UI; insufficient-resource buttons render disabled with explicit "Не хватает: 🏃 N" line. After 0.2.1g (4.48.4.1) all skills work uniformly — `energy_max_skill` no longer special-cased. Skill count grew over time: 8 (0.2.1g) → 9 (`banking_interest_rate`, 0.2.2 / 4.49.1.0) → 11 (+ `loan_capacity` + `loan_interest_reduction`, 0.2.2 / 4.49.2.0) → 12 (+ `inspiration`, 0.2.3 / 4.27) → 13 (+ `money_saving`, 0.2.3a / 4.20) → 14 (+ `earnings_boost`, 0.2.4a / 4.23) → 15 (+ `backpack_skill`, 0.2.4b / 4.50.0) → 16 (+ `trader`, 0.2.4h / 4.28 — money trilogy completed) → 17 (+ `energy_regen_skill`, 0.2.4i / 4.21 — Speed split) → 20 (+ 3 `energy_optimization_{adventure,gym,work}`, 0.2.4j / 4.22 — energy reduction trilogy симметрично move_opt) → 23 (+ 3 forge-навыка `forge_steps_saving` / `forge_money_saving` / `forge_repair_quality`, 0.2.6a / 4.60 — экономия шагов/золота + множитель качества ремонта; любой ≥1 разблокирует локацию Кузница) → 24 (+ `forge_speed`, 0.2.6d / 4.59.4.1 — −1%/lvl к длительности ремонта/крафта; тоже разблокирует Кузницу). Порядок entries в `_GYM_SKILL_DISPLAY` определяет порядок рендера в web и CLI: stamina (1), energy_max_skill (2), energy_regen_skill (3 — позиция после Speed split в 0.2.4i), speed_skill (4), luck_skill (5), move_optimization_* (6-8), neatness_in_using_things (9), money trilogy `money_saving` / `earnings_boost` / `trader` (10-12), bank-навыки `banking_interest_rate` / `loan_capacity` / `loan_interest_reduction` (13-15), `inspiration` (16), `backpack_skill` (17), forge-навыки `forge_steps_saving` / `forge_money_saving` / `forge_repair_quality` / `forge_speed` (CLI пункты 21-24). Аналогично CharLevel allocation menu (`level.py:menu_skill_point_allocation` и web `_SKILL_DISPLAY`) расширен с 4 до 5 опций — `skill_energy_regen` рядом с `skill_speed`.
10. **Energy max as derived value (0.2.1g).** `state.energy_max` is no longer read directly in game logic. The canonical source is `bonus.compute_energy_max(state) = 50 + gym.energy_max_skill + equipment_energy_max_bonus + steps.daily_bonus + char_level.skill_energy_max`. The dataclass field `state.energy_max` is kept only for save-format backward compat (CSV/Sheets column), updated at load via `compute_energy_max`. Use `compute_energy_max(state)` for any cap check / display / clamp. The off-by-one reverse-calc `_energy_max_skill_level` was removed. Gym skill key was renamed `'energy_max'` → `'energy_max_skill'` to match the dataclass field name (consistent with `speed_skill`, `luck_skill`); legacy save migration in `state.from_dict` auto-converts old key.

Known follow-up: double-claim race on stale Sheets state — see TASKS.md `4.48.5.1`.

### Mutation helpers: `actions.py`

Non-trivial mutations go through `actions.py` to keep invariants in one place:

- `try_spend(state, steps, energy, money) → bool` — atomic check & deduct. Returns False without mutation if any resource is insufficient.
- `start_work(state, work_type, salary, hours, start, end)` — populate `state.work` for a session.
- `start_training(state, skill_name, time_end, timestamp)` — populate `state.training`.
- `start_adventure(state, name, start_ts, end_ts)` — populate `state.adventure`.

### Gameplay module map

- `locations.py` — dispatch layer between `game.py` and each location module; `icon_loc(state)` maps `state.loc` to an emoji.
- `gym.py`, `work.py`, `adventure.py`, `shop.py`, `inventory.py`, `equipment.py`, `bank.py`, `forge.py` — one per location/menu. `work_check_done(state)` and `skill_training_check_done(state)` are polled from the main loop to finalize timer-based activities; if you add another timed action, follow the same "check on every tick" pattern.
- `shop.py` (зонтичная задача 4.7 — Shop framework + каталог items, L/M/todo). **Текущее состояние (partial, polished 0.2.5f 21.05.2026):** работающий buy flow для food/water (Чизбургер +5 эн, Кофе +25 эн) и shoes (3 grade'а C/B/A). Pure helper `_buy_item(state, item, cost) -> bool` (атомарная покупка с money + inventory_full checks, log_event('item_bought')). Корректно интегрирован с прокачкой навыков: `apply_money_saving` (4.20) применяется во всех 5 ценах; `backpack_skill` (4.50.0) → `inventory_full` check; `log_event` (4.6); `_is_broken` (4.61) — food/drink с empty quality list не считаются broken; `forge._item_key` returns None для food (нет grade) → cheeseburgers НЕ попадают в craftable groups. **Известные ограничения (отложены до 4.7):** (1) **Consumables без consume mechanic** — Чизбургер/Кофе имеют `characteristic='energy', bonus=5/25`, но `inventory.py` не имеет «Use item» flow, поэтому items лягут мёртвым грузом. С 0.2.5f показывается yellow warning «⚠ Расходники пока БЕЗ ЭФФЕКТА (consume mechanic ждёт 4.7.1)» перед покупкой. (2) **Stubs шапка/куртка/штаны/перчатки** — `_clothes_stub` печатает «Тестовая - {label}» и возвращается, без mutation (benign). (3) **`shop_menu_equipment` + `shop_menu_sell_items`** — `pass`-заглушки, в planned 4.7.0 framework. (4) **Display polish (0.2.5f):** shoes показывают «+ N к Stamina» (flat, как реально считается в `equipment_bonus.equipment_stamina_bonus`); раньше показывал misleading «+ N % шагов».
- `bank.py` (since 0.2.2 / task 4.49) — депозиты + кредиты. **Capitalize-on-change**: `accrue_deposit(state)` / `accrue_loan(state)` капитализируют накопленные проценты по ТЕКУЩЕЙ ставке за `now - last_interest_ts` перед любым событием, меняющим тело. Триггеры: top-up / withdraw / take_loan / repay. **Retro-bonus exploit ОТКРЫТ намеренно (since 0.2.4z / tasks 4.49.1.2 + 4.49.2.3)** — accrue **НЕ** срабатывает на skill-up (`banking_interest_rate` / `loan_interest_reduction`); следующая mutation применит НОВУЮ ставку ретроактивно ко всему накопленному с `last_interest_ts` периоду. Stimulus прокачивать скиллы: «открыл депозит → прокачал → собрал retro-проценты» / «взял кредит → прокачал reduction → отдал меньше». Симметрично earnings_boost в work (4.23 — `state.work.salary` базовая, recompute с текущим уровнем скилла в момент финализации). До 0.2.4z hook закрывал exploit (4.49.1.1 / 4.49.2.1 хук в `gym.skill_training_check_done`) — capitalize по СТАРОЙ ставке перед инкрементом; снято как намеренный design choice. **Полное обоснование + numerical examples + maintainer warning** — [`docs/bank_retro_bonus.md`](docs/bank_retro_bonus.md) (если кто-то увидит exploit и захочет «починить» — сначала прочитать док). Скиллы-gate'ы: `banking_interest_rate >= 1` гейтит депозит (`can_open_deposit` + `can_withdraw`), `loan_capacity >= 1` гейтит кредит (`can_take_loan`); `loan_interest_reduction` снижает ставку с дефолтных 100% годовых. Display: 2 знака в Bank-меню, `:,.0f` в остальных меню; `state.money: float` (а не int) чтобы копейки текли между кошельком и депозитом без потерь.
- `drop.py` — item drop logic for Adventure. **Полная справка по механике дропа + вероятности по всем walk × luck + дизайн pity (4.19) — [`docs/drop_mechanics.md`](docs/drop_mechanics.md).** Grades are `c/b/a/s/s+`, weighted by `drop_percent_*` constants. `current_luck(state)` is computed at the moment of the drop (no module-level pinning). Since 0.2.4f (4.29-replacement) функция `compute_grade_probabilities(adv_name, state) -> dict[str, float]` возвращает аналитические вероятности каждого грейда (включая 'nothing') с учётом текущего luck — используется в Adventure menu для отображения %; формула: P(grade_i) = P(gate) × Σ_{r=1..min(T_i, N)} (1/N) × ((N-r)/N)^{n-1}, где N=100-luck. MC parity tests в `tests/test_drop.py` (10k×7 итераций, ~200ms) защищают от рассинхрона аналитики и реальной `one_item_random_grade`. Since 0.2.4g (balance fix) S+ threshold перестал быть глобальным — есть per-adventure overrides: `drop_percent_item_s_=15` для walk_20k (рядовой бонус среди 3 тиров), `drop_percent_item_s_walk_25k=20` (мягкий буст), `drop_percent_item_s_walk_30k=35` (endgame walk — чёткое позиционирование «специально за S+»). До 0.2.4g walk_30k был ловушкой для игрока (всего +1.41% S+ vs walk_25k за +20% цены), теперь walk_30k = ~2× преимущество по S+. Since 0.2.4c (task 4.50.1) `Drop_Item.item_collect` имеет три ветки на момент дропа: (1) `not bonus.inventory_full(state)` → append + log_event('drop'); (2) full + `state.pending_drop is None` → находка уходит в `state.pending_drop` (Optional[dict]) + одноразовое info-сообщение «🎒 Инвентарь полон. Находка ждёт решения...» + log_event('drop_pending'); (3) full + pending уже занят → forced sale (`state.money += new.price`, старый pending не трогается) + log_event('drop_force_sold'). Pending — это **один** слот на хранение, не очередь (D-iii: при коллизии новый авто-продаётся). **Since 0.2.6 (4.48.5.1.1):** три `log_event` в `item_collect` идут через `history.emit_or_defer(deferred_events, ...)` — `Drop_Item.item_collect(..., deferred_events=None)` и `Adventure.adventure_check_done(..., deferred_events=None)` принимают опциональный буфер; web-финализатор копит события и логирует их (history + triumph `register_event`) только ПОСЛЕ OK save commit, на STALE rollback'е выбрасывает — без phantom-записей в history (искажали triumph-backfill) и без phantom triumph-инкремента. CLI (`deferred_events=None`) логирует сразу. Resolve в CLI через `inventory.inventory_menu` — prompt в начале меню при `pending != None` (3 опции: продать предмет №N + положить pending, продать pending за base price, skip — pending остаётся). Auto-collect при освобождении места (продажа / прокачка `backpack_skill` / снятие экипировки) — через `bonus.auto_collect_pending_drop(state)`, вызывается на каждом тике CLI main loop'а в `game.py` И на каждом рендере `_dashboard_context` в web (since 0.2.4d / 4.50.2) с `persist_state_to_cloud()` при transition. Web UI (since 0.2.7e — финальный дизайн): pending-баннер «🎁 Найдена находка» вверху dashboard'а с одной кнопкой «Продать находку» (`sell_new`); строки инвентаря — обычные Надеть/Продать (sell_existing убрана из UI), освобождение слота → `auto_collect_pending_drop`. Баннер **не закрывается вручную** (кнопка «Отложить» и runtime-флаг `pending_drop_banner_dismissed` удалены — было в 0.2.7c, отменено в 0.2.7e в пользу неисчезающего баннера). `_build_pending_drop_view` отдаёт `active`/`banner` (оба True при pending) + `item` (`price` сырой + `sell_price` через `apply_trader`); inventory `<details>` авто-открывается пока pending активна. Авто-подбор показывает баннер «🎁 Находка подобрана» в session-events. Endpoints: `/web/drop/sell_new` (Form) + `/api/drop/{sell_existing,sell_new}` (JSON) + `/web/drop/sell_existing` (Form) оставлены рабочими как API-зеркала, из web-UI не вызываются (`/web/drop/skip` удалён). Persist в Sheets через round-trip flat-key 'pending_drop' (None для legacy сейвов до 0.2.4c). **Since 0.2.6l (task 4.65 — Drop-modifier):** `item_type()` — **взвешенная выборка** (`random.choices(_DROP_TYPES, weights)`), заменила прежний «5 кубиков max + пре-гейт рюкзака» (0.2.6j). Quirk «ничья кубиков → None» убран — тип всегда валиден. Веса = base + relative luck-буст к *нужным* типам (completion-incentive): база рюкзак `BACKPACK_DROP_CHANCE×100`=5, прочие 5 типов по 19. Нужность типа = дистанция до S+ по **грейду** (`_type_gap = 5 − best_owned_tier`, тиры пусто=0/C=1/…/S+=5, по equipment+inventory); кольца — вариант R2 (gap по ХУДШЕМУ из двух надетых пальцев, инвентарь игнор). `_type_weights`: худший слот (`max_gap`) → полный `current_luck`-буст, остальные `luck × gap / max_gap`; `max_gap=0` (всё S+) → база. luck сырой, линейно. Type-boost НЕ зависит от грейда, доступного в текущем adventure (всегда дистанция от S+ — упрощение). Хелперы `_item_grade_tier` / `_best_owned_tier` / `_type_gap` / `_type_weights`. `item_collect` ветвит на backpack: characteristic=`'backpack_capacity'`, bonus=`BACKPACK_GRADE_SLOTS[grade]` (cosmetic — реальная вместимость в `bonus.backpack_capacity` берётся от грейда). Рюкзак (4.51) надевается в equipment-слот `back` («Спина»), даёт +2/4/6/8/10 слотов инвентаря по грейду.
- `level.py` (`CharLevel`), `characteristics.py` (`save_characteristic`), `bonus.py`, `equipment_bonus.py`, `skill_bonus.py` — stat/bonus math. All take `state` explicitly. Since 0.2.3 (task 4.27 — «Обучение» / Inspiration skill) `CharLevel.calculate_level_from_total_used_steps` и `progress_to_next_level` используют helper `_effective_xp() = state.steps.total_used + int(state.steps.xp_bonus)` вместо raw `total_used`. `xp_bonus: float` — accumulator forward-only multiplier'а: на каждый успешный `actions.try_spend(steps=N)` добавляется `N * gym.inspiration / 100.0` (если skill > 0). Property `total_used_steps` остаётся raw (отображение «Total used» в status_bar показывает реальные шаги, без bonus). При `inspiration=0` (default / старые сейвы) `xp_bonus` не растёт → поведение неизменно. Since 0.2.3a (task 4.20 — «Экономия денег» / Money Saving skill) `bonus.apply_money_saving(cost, state) -> float = round(max(0.0, cost * (1 - gym.money_saving / 100)), 2)` оборачивает `cost['money']` в Gym training (4 точки в `gym.py`) и Shop purchases (4 точки в `shop.py`); web `_build_gym_skills` + `_validate_and_apply_training` тоже учитывают скидку. Линейная формула, при skill=100 → cost=0, при skill>100 → clamp до 0. `actions.try_spend(money: float)` — сигнатура поднята с int. НЕ применяется к Bank operations / Work salary / Adventure rewards. Since 0.2.4a (task 4.23 — «Бонус к зарплате» / Earnings Boost skill) симметричный helper `bonus.apply_earnings_boost(salary, state) -> float = round(salary * (1 + gym.earnings_boost / 100), 2)` обёрнут в **5 display точек CLI work.py** (ask_hours / add_working_hours / check_requirements preview / + final credit в work_check_done) и **2 web точках** (`_build_hour_options` / `_build_work_vacancies`). `state.work.salary` остаётся **базовой** — bonus применяется на лету (recompute model: прокачал во время смены → next render preview обновится, при завершении применяется текущий уровень). Линейная без cap, на skill=100 удвоение дохода. НЕ применяется к продаже предметов / Bank / Adventure. `_dashboard_context` pre-computes `work_effective_salary` / `work_effective_total` для шаблона Active Sessions. Since 0.2.4j (task 4.22 — Energy Optimization per-activity) helpers `bonus.apply_energy_optimization_{adventure,gym,work}(energy, state) -> int` снижают энергозатраты на 1%/level. Линейная формула `max(1, int(base × (1 − skill/100)))` — clamp min=1 (никогда не бесплатно). Match с move_optimization_* / money_saving / trader. Особенность Work: применяется к **TOTAL** energy (per_hour × hours), НЕ per-hour — это убирает плато на low-base работах (watchman 4 эн/ч: per-hour rounding давал бы saving=1 на skill 1-25, total approach даёт линейную экономию). Адресует chicken-and-egg progression: без этого helper Stamina lvl 19 (cost 95 эн) физически недоступна игрокам с energy_max < 95. max_hours_by_energy в Work UI и web/main.py:_max_work_hours использует loop (max 8 итераций) чтобы найти maximum h при котором optimized total ≤ state.energy. Since 0.2.4i (task 4.21 — Speed split) helper `bonus.energy_regen_interval(base_seconds, state) -> int` отделяет скорость регенерации энергии от длительности активностей. До 0.2.4i `functions.energy_time_charge` использовал `speed_skill_equipment_and_level_bonus(60, state)` — тот же helper что для длительности активностей; это связывало две несвязанные механики. Теперь regen зависит только от `state.gym.energy_regen_skill + state.char_level.skill_energy_regen` (equipment не учитывается в V1, будет в V2 / задача 4.57 через characteristic='energy_regen'). Длительность активностей (`speed_skill_equipment_and_level_bonus`) осталась без изменений — Speed, equipment Speed и CharLevel skill_speed продолжают работать. Migration legacy сейвов: оба новых поля начинают с 0 (β-вариант — игрок прокачивает с нуля, не копируем speed_skill). Since 0.2.4h (task 4.28 — «Торговец» / Trader skill) helper `bonus.apply_trader(price, state) -> float = round(price * (1 + gym.trader / 100), 2)` — third leg of экономической трилогии (4.20 money_saving / 4.23 earnings_boost / 4.28 trader). Применяется во **всех 4 точках продажи** через единый helper: `inventory._sell_item_at_index` (обычная продажа), `inventory._resolve_pending_drop_sell_existing` (продажа existing item в pending-drop resolve), `inventory._resolve_pending_drop_sell_new` (продажа самой находки), `drop.Drop_Item.item_collect` forced-sale ветка (при full inventory + pending occupied). Без cap, на skill=100 — удвоение. Refund signatures поднялись `int` → `float` (round до 2 знаков) для consistency с другими money-helper'ами. Since 0.2.4b (task 4.50.0 — «Размер инвентаря» / Backpack Skill) `bonus.BASE_BACKPACK_CAPACITY = 10` + helper'ы `backpack_capacity(state) = BASE + state.gym.backpack_skill + backpack_slots_bonus(state)` и `inventory_full(state) = len(state.inventory) >= cap` (`>=`, а не `==` — backwards-compat: legacy сейвы с переполненным инвентарём не могут добавлять новые предметы пока не освободится слот). **Since 0.2.6j (task 4.51 — Рюкзак)** `backpack_slots_bonus(state)` = вклад надетого рюкзака (equipment-слот `back`): `BACKPACK_GRADE_SLOTS[grade]` (C/B/A/S/S+ → +2/4/6/8/10) если рюкзак надет И не сломан (`_is_broken` quality 0 → 0). Рюкзак — обычный equipment-предмет (item_type `backpack`), дропается в Adventure флэт 5% (пре-гейт в `drop.item_type` ДО обычного выбора 5 типов, `BACKPACK_DROP_CHANCE`; grade-гейт не трогаем). characteristic `backpack_capacity` (НЕ optimizable → loadout не трогает). Износ/ремонт/craft — как вся экипировка. Слот `back` добавлен через весь стек (state.Equipment+round-trip, equipment_bonus, inventory durability, forge, equipment.py CLI слот 7, sync_diff, web `_ITEM_TYPE_TO_SLOTS`/labels). Display «+N слотов» (CLI + web). НО в loadout-оптимизаторе/пресетах слот `back` **намеренно отсутствует** (вместимость не optimizable). Capacity-check встроен в `shop._buy_item` и `equipment._unequip` (возвращают False/None при full), UI пред-проверяет `inventory_full` чтобы напечатать корректное сообщение «Рюкзак полон (N/cap). Освободи слот...». `_equip_from_inventory` (swap) — net-zero, проверку пропускает. Display: CLI `inventory_menu` / `sold_item` / `Equipment.inventory_view` показывают `(N/cap)`, web `_status_fragment.html` — `🎒 Инвентарь (N/cap)` через context-key `inventory_capacity`. Since 0.2.4c (task 4.50.1) `bonus.auto_collect_pending_drop(state) -> Optional[dict]` — pure helper для случая B (cap расширилась пока pending активен): если `state.pending_drop != None and not inventory_full(state)` → append в инвентарь + clear pending + log_event('drop_auto_collected') + return item; иначе None. Идемпотентен. Вызывается в `game.py` main loop на каждом тике (между `skill_training_check_done` и `status_bar`) и печатает one-shot уведомление «🎁 Освободилось место в рюкзаке. Находка ... добавлена в инвентарь» при transition. Since 0.2.3b (task 4.20.1) **все** money / cost / wallet displays унифицированы на `:,.2f` через `functions_02.format_money(amount, decimals=2)` — единая точка форматирования (registered as Jinja global для web). Wallet теперь показывает `2,038.00 $` вместо `2,038 $` — копейки видны везде где могут появиться (после Bank operations, money_saving discount, и т.д.).
- `loadout.py` (since 0.2.4q / task 4.63) — Equipment Auto-Optimizer + Presets. **Pure helpers** без UI и без Sheets-зависимостей; UI (Equipment menu пункты 7 и 8) в `equipment.py`. Решает chicken-and-egg прогрессию (Stamina lvl 19+ требует 95 эн при energy_max=65, без переодевания в +energy_max equipment физически невозможно). **Phase 1 (4.63.1, 0.2.4q) Auto-Optimizer:** `find_optimal_loadout(state, characteristic) -> dict[slot, item|None]` — для каждого слота max bonus по characteristic из пула (equipment + inventory). 4 поддерживаемых characteristic'а — `OPTIMIZABLE_CHARACTERISTICS = ('stamina', 'energy_max', 'speed_skill', 'luck')` (asymmetria: на equipment item это `luck`, в Gym skill — `luck_skill`; legacy naming). Ring-слоты: top-2 X-rings → finger_01 (best) / finger_02 (second). **Keep-current semantics** (критичное решение): для слотов без matching X-item оставляем current equipped — без этого optimizer был бы destructive (target[slot]=None → apply unwear → потеря бонусов других characteristic'ов без причины). `preview_loadout_diff(state, target)` — identity-based diff (`is` comparison). `apply_loadout(state, target) -> (success, warnings)` — atomic two-phase (unwear changed → wear target) с capacity check ПЕРЕД (peak = current_inv + items_to_unequip > cap → reject без мутаций). **Phase 2 (4.63.2, 0.2.4r) Presets:** новое поле `state.equipment_presets: dict[name, dict[slot, item_snapshot|None]] = field(default_factory=dict)` с round-trip через flat-key 'equipment_presets' ({} для legacy сейвов). Snapshots — deep-copy items на момент save (preset переживает продажу original). Identity matching на apply через `_match_preset_item` по (item_name, item_type, grade, characteristic[0], bonus[0]) — quality/price игнорируются (могут меняться от Repair/wear). Lost items (Q3 design): skip + warning + keep current equipped в этом слоте (не unwear). API: `save_preset(state, name)` (strip + non-empty validation), `delete_preset`, `list_presets` (sorted case-insensitive), `resolve_preset_to_loadout` (snapshot → current refs + warnings). Apply переиспользует `apply_loadout`. log_event: `loadout_optimized` (5 fields), `preset_saved` / `preset_applied` / `preset_deleted`. Persist НЕ делается — следует существующему equipment pattern (wear/unwear тоже не зовут save_characteristic, игрок жмёт `s`). **Phase 3 (4.63.3) Web UI** — blocked by 4.48.6.
- `forge.py` (since 0.2.4m / task 4.59) — Кузница / Blacksmith. **MVP закрыт в 0.2.4o** (4.59.0 infra + 4.59.1 Repair + 4.59.2 Crafting; 4.59.3 Gems — deferred low-priority, см. TASKS.md). **Forge skills + unlock gate (4.60, 0.2.6a):** локация **заблокирована**, пока любой из 3 forge-навыков (`forge_steps_saving` / `forge_money_saving` / `forge_repair_quality`) не прокачан до ≥1 (gate в `locations.forge_location`, симметрично Банку). `forge_steps_saving` / `forge_money_saving` дают −1%/lvl к шагам / золоту в Repair И Crafting (энергия без скидки — energy-навык не вводили) через wrappers `repair_cost_effective(percent, state)` / `crafting_cost_effective(source_grade, state)` (money → float после `bonus.apply_forge_money_saving`). `forge_repair_quality` — множитель `bonus.forge_repair_multiplier(state) = 1 + lvl/100` к восстановленному quality в `repair_item` (restored = `percent × multiplier`, БЕЗ округления — Restorer-триумф считает дробный буст точно). **Таймеры (4.59.4.1, 0.2.6d):** Repair/Crafting — таймерные сессии (как Work/Gym/Adventure), НЕ instant. Длительность: ремонт `repair_duration` = 1ч/1% (`REPAIR_SECONDS_PER_PERCENT=3600`); крафт `craft_duration` по грейду источника (`CRAFT_HOURS_BY_GRADE` C/B/A/S=1/4/8/12ч). Навык `forge_speed` (`bonus.apply_forge_speed`, −1%/lvl, clamp 60с) ускоряет обе операции. **Capture-into-session:** `start_repair_session` / `start_craft_session` списывают ресурсы СРАЗУ + физически убирают предмет(ы) из инвентаря/экипировки в `state.forge.payload` (снапшот, round-trip flat-key `forge_session`); `forge_check_done(state)` финализатор (save-first atomic + STALE-rollback, mirrors `work_check_done`) применяет результат на таймере (repair → quality boost; craft → новый item) + `log_event` на ФИНИШЕ (Restorer/Investor триумфы там) + возвращает предмет в инвентарь. Хук в `game.py` main loop; CLI `forge_menu` блокирует новую операцию при active + показывает countdown. `repair_item`/`craft_item` остались instant-apply (для финализатора). **Web (4.59.4.2, 0.2.6e):** web полностью таймерный — endpoints `/web|api/forge/repair` + `/web/forge/craft` зовут `start_*`; `forge_check_done` в `_dashboard_context`; `_build_forge_view` при active скрывает формы + рендерит «Идёт» + countdown; forge-сессия в «⏱ Активные сессии» + `modal-forge-session` (4.48.10). Закрыта зонтичная 4.59.4 (CLI+web). **Repair (4.59.1):** `repair_cost(percent) = (1000×p шагов, 100×p $, 10×p эн)` — фиксированная линейная цена без scale-by-grade (намеренный gating: для C/B дороже чем дроп нового, для S/S+ экономически выгодно). `max_repair_percent(state, item)` использует **effective** per-1% cost (`repair_cost_effective(1, state)`) с zero-guard — forge-скидки повышают доступный кап. `repair_candidates(state)` — equipment slots + inventory items с `quality < 100`, sorted by quality asc. `repair_item(state, item, percent)` — атомарный try_spend → quality += percent (clamp 100) → recalc price → `log_event('item_repaired')`. **Crafting (4.59.2):** Recipe Variant A — 2 идентичных по `(item_type, characteristic, grade)` → 1 next grade, quality = avg, bonus = `GRADE_BONUS_VALUE[next]`, price = `int(quality × GRADE_PRICE_MULTIPLIER[next])`. `crafting_cost(grade) = tier × (1000, 100, 10)` где tier(C)=1 … tier(S)=4; s+grade → `(0,0,0)` cap. `find_craftable_groups(state)` — auto-scan, key `(item_type, characteristic, grade)` count≥2. `craft_item(state, a, b)` — атомарный: validate (identity ≠, same key, не s+grade, ресурсы, inventory не overflow — projected = current - n_inv_sources + 1) → try_spend → auto-unequip equipped → удаление inventory sources (identity by `is`) → новый item в inventory → `log_event('item_crafted', was_equipped=N)`. UI: explicit `yes` подтверждение (защита от случайного `y`), equipped warning, sell price учитывает trader skill (4.28). **Цветовая схема:** все cost-displays через хелперы `_c_steps` (LIGHTCYAN_EX), `_c_money` (LIGHTYELLOW_EX), `_c_energy` (GREEN) + сводный `_fmt_cost`. `[Слот]` префикс для equipped — GREEN, `[Инвентарь]` без цвета. `_c_quality` — 3-зонная шкала (`<20` red, `<50` yellow, `≥50` green) для визуального scan-а Repair-списка. **Поломка:** quality снижает только `price`, **не** `equipment_*_bonus` (на qual=0 предмет даёт полный бонус) — реальная механика поломки вынесена в **task 4.61** (Low priority, deferred). **Web (4.48.11, 0.2.6c):** Repair + Crafting доступны через web (instant; таймеры — 4.59.4) — см. «Web: Кузница» в разделе Web mutation endpoints.
- `adventure_data.py` — static data table for adventures. Adventure `__init__` copies entries before applying `move_optimization` so the table itself is never mutated. Since 0.2.4f (4.29-replacement) поле `drops: [(grade, threshold), ...]` — упорядоченный список тиеров для каждого приключения, импортирует `drop_percent_item_*` константы из drop.py. Единый источник правды для `drop.py:one_item_random_grade()` и `compute_grade_probabilities()`. Мёртвые поля `drop_items` (устаревшая `{ring, necklace}` — реально drop.py:item_type сэмплит 5 типов независимо от hard'а) и `drop_grade` (set без порогов) удалены. **Unlock chain (4.34, 0.2.6):** константы `ADVENTURE_UNLOCK_THRESHOLD = 3` + `ADVENTURE_PREREQ` (adv → prereq adv) + `ADVENTURE_RU_LABELS` — единый источник цепочки разблокировки для CLI (`adventure._render_adventure_menu`) и web (`_build_adventure_view` / `_validate_and_apply_adventure`); раньше порог `3` дублировался литералом в 7 местах CLI + dict `_ADVENTURE_UNLOCK` в web. Прогресс к разблокировке показывается глиф-баром `▰▱` (переиспользован `triumphs._format_progress_bar`, width=3) **только у первой запертой прогулки** (`▰▰▱ 2/3 (67%)`); более глубокие — «🔒 заблокировано». UI-only, логика разблокировки не менялась. NB: CLI-выбор пункта меню исторически не enforce'ит locked (косметика); web — enforce через `_validate_and_apply_adventure`.
- `skill_training_data.py` (since 0.2.3e / task 1.3.2) — static lookup-таблица стоимости прокачки Gym-навыков (1..30 уровни) + helper'ы `get_skill_training(level)` (экстраполяция формулой для level > 30) и `get_energy_training_data(level)` (wrapper для Daily Bonus). Pure data + pure helpers, без state-зависимостей. Импортёры: `gym.py`, `web/main.py`. Раньше жил в `characteristics.py:165-390`, вынесен по аналогии с `adventure_data.py` — разделение lookup-данных и state-логики.
- `functions.py` / `functions_02.py` — cross-cutting helpers (`status_bar`, `energy_time_charge`, `char_info`, date/time helpers). `functions_02.format_timedelta(td)` (since 0.2.1i) is the canonical countdown formatter for work / training / adventure timers — produces `Y г M мес W нед D д H:MM:SS` (с 0.2.6 — пробел между числом и единицей: «3 нед», читабельнее). The JS `formatRemaining(seconds)` in `web/templates/dashboard.html` mirrors it constant-for-constant (year=365d, month=30d, week=7d, **тот же пробел**) so CLI and web show identical strings for the same state — при изменении формата правь ОБА. Existing `functions_02.time(x)` (minutes → "H час M мин." with colorama) is a separate display helper for activity costs in CLI gym/work/adventure cost menus. Since 0.2.4s (4.48.3 polish) — добавлен `functions_02.format_minutes(x: int) -> str` — plain-text близнец `time(x)` без colorama escape sequences для web/HTML контекста (Jinja global, используется в Adventure section template). CLI продолжает использовать `time(x)` с colorama, web — `format_minutes`. Та же логика разбивки на units (≤60 → 'X мин.' / <1дн → 'H ч. M мин.' / <30дн → 'D дн. H ч. M мин.' / <1г → '... мес. ...' / иначе 'Y г. ...'). Since 0.2.7f — `functions_02.format_shift_hours(total_hours) -> str` — компактный формат **отработанного времени** для сообщения о завершённой смене (CLI `work.py:work_check_done` + web баннер «Смена завершена» в `_build_session_events_view`): `>= 3 дней` → `'Nд Mч (Tч)'` (дни+часы + всего часов в скобках, чтобы виден и масштаб в днях, и точная цифра), `< 3 дней` → `'Nд Mч'`, `< 1 дня` → `'Mч'` (без ведущего '0д'). Единый источник для обеих платформ (CLI/web parity — раньше CLI вообще не показывал часы, только сумму). Отличается от `format_hours` (тот — «Y г. MO мес. D дн. H ч.» для сводки **активной** смены в web summary).
- `settings.py` — `debug_mode` flag consumed by many modules for verbose logging.
- `version.py` (since 0.2.4 / task 4.6) — единая точка правды для `VERSION` constant. Импортируется в `game.py` (CLI startup print), `web/main.py` (FastAPI app version), `history.py` (поле `game_version` в каждом event'е). Раньше дублировалась в game.py и web/main.py — каждый bump требовал двух правок. Теперь bump в одном месте.
- `persistence.py` (since 0.2.4y / tasks 1.3.3 + 1.4.2, 20.05.2026; refactored 1.4.3 / 0.2.5, 20.05.2026) — save/load layer для game state. Содержит: `save_characteristic() -> Literal["OK","STALE"]` (Sheets save_safe + локальный state.json), `load_state_json()` (primary, 1.4.3), `load_characteristic()` (legacy CSV reader, удалится в 1.4.3.1), `load_data_from_google_sheet_or_csv()` (orchestrator: Sheets primary + локальный fallback), `_load_local_fallback()` (state.json → CSV → empty), `handle_stale_prompt() -> Literal["reload","force","cancel"]` (CLI STALE UX, 3 options + double confirm для Force), и **unified parser** `_parse_value(value, key=None)` — priority chain (non-string → empty → JSON → ast.literal_eval → manual int/float/bool → datetime для DATE_KEYS → plain string). С 1.4.3 (0.2.5) save пишет **`state.json`** через `_json_default` (полный JSON-blob с indent=2), CSV-write убран. `_DATETIME_KEYS` пишутся как str (strftime), читаются обратно через strptime в `load_state_json`. **Defense против регрессии (4.48.5.3 / 0.2.5c):** `save_characteristic` в самом начале (после `game.state is None` check) вызывает `apply_steps_log_max_merge(game.state)` — гарантирует что любой save (CLI, web mutation, финализатор) пишет в Sheets текущий максимум steps.today по log'у. Web RAM может устареть относительно steps_log (apply_steps_log_max_merge запускается ТОЛЬКО в `try_reload_state` на GET /); без этой защиты web mutation могла бы записать stale today поверх свежего → real-world incident 20.05.2026. Silent-fail если steps_log недоступен. Constants `STATE_JSON_PATH` / `CHARACTERISTIC_CSV_PATH`. `characteristics.py` остался state lifecycle: `_GameContainer` + `game` singleton + `init_game_state` + `apply_steps_log_max_merge`.
- `history.py` (since 0.2.4 / task 4.6) — append-only лог значимых игровых событий. Единая точка `log_event(event_type, **payload) -> None`. Dual-write: local `history.jsonl` (sync, fail-resistant — печатает warning на I/O error) + Sheets `history` лист (best-effort через `HistoryLogRepo.append`, fail-silent). 22 типа событий в 15 mutation-сайтах. См. также Persistence layers выше.
- `triumphs.py` + `triumphs_data.py` + `triumphs_menu.py` (since 0.2.5j..0.2.6 / task 4.62 «Triumphs», 22-25.05.2026) — Destiny-2-inspired achievements: tiered progression + score + seals/titles + pinned status bar + claim queue + cross-device backfill. **Полное описание системы + каталог всех 61 triumph'а на момент 0.2.6i** — отдельный документ [`docs/triumphs.md`](docs/triumphs.md) (концепция, архитектура, state schema, mechanics, recipe для добавления новых). Здесь — короткий operational overview. **Архитектура — 3 модуля:** `triumphs_data.py` (static catalog `TRIUMPHS` + `CATEGORIES` + `SEALS` + `POINTS_PER_TIER=10`), `triumphs.py` (pure engine, без UI / без Sheets), `triumphs_menu.py` (CLI UI: 3-level navigation main → category → detail). **State schema (4.62.0.1 + 4.62.4 + 4.62.7.6):** 6 полей в `GameState` с round-trip — `triumphs: dict[id, {tier, count, unlocked_at}]` (sparse, + pseudo-entry `__seal__` для seal acknowledged tracking), `pinned_triumphs: list[str]` (≤3 cap), `title: Optional[str]`, `triumphs_backfill_dismissed: bool` (one-shot CLI prompt), `unclaimed_unlocks: list[dict]` (claim queue с полями `triumph_id`, `tier`, `unlocked_ts`, `kind: 'triumph'|'seal'`), `unclaimed_banner_dismissed_date: str` (4.62.7.6 — игровой день dismiss'а web-баннера). Плюс `StepsState.total_walked` (4.62.1.1.1 — Wayfarer), `StepsState.daily_streak_record` (4.62.1.9 — On Fire), `GameState.days_played` (4.62.1.9 — Dedicated). **Два типа triumph'ов:** metric-based (`spec['metric']` lambda читает state — auto-unlock через `init_metric_check`) и event-based (`spec['event_hooks']` + `count_delta` — backfill через `backfill_from_history`). **Auto-hook (4.62.0.2 + 4.62.3):** `history.log_event` вызывает `register_event` → `append_unclaimed` → `check_seal_unlocks` → `append_unclaimed` для seal unlocks. Try/except / silent fail / lazy import. Без отдельных hook'ов в mutation sites. **Catalog на 0.2.6i:** 61 triumph в 10 категориях (🏃 Steps 2 — Marathoner metric `total_used` потрачено + Wayfarer metric `total_walked` реально пройдено, оба tiers 100k/500k/1M/5M/10M, 4.62.1.1.1; `steps`-seal «Marathoner» требует capstone обоих / 🔋 Energy 4 / 🗺 Adventures 8 / 🏋 Gym 21 / 🏭 Work 6 — Hard Worker + 4 per-vacancy + Iron Worker metric-based на `state.work.longest_shift_hours` tiers 24/72/168/336/720h / ⭐ Progression 1 — Veteran metric `state.char_level.level` tiers 5/10/15/20/25/30, 4.62.1.8 / 💎 Drops 6 — Collector + 5 per-grade event-based на `[drop, drop_pending, drop_force_sold]` с grade-фильтром, tiers 10/50/100/250/500/1000, 4.62.1.7 / 💰 Money 1 — Investor event-based accumulator `cost_money` из `skill_train_start`, tiers 1k/10k/50k/250k/1M, без seal, 4.62.1.12 / 🔥 Streak 2 — Dedicated metric `state.days_played` (уникальные активные дни) tiers 1/7/31/184/365 + On Fire metric `state.steps.daily_streak_record` (макс стрик ПОДРЯД 10k+ дней, rollover-max от `daily_bonus`, freeze-ready для 4.36) tiers 3/7/14/21/31, без seal, 4.62.1.9 / 🔨 Forge 2 — Restorer event-based accumulator восстановленного quality (`to_quality−from_quality` из `item_repaired`, НЕ число кликов) tiers 25/50/100/500/1000 + Crafter event-based счётчик скрафченных предметов (`item_crafted`, +1) tiers 5/10/25/50/100, без seal, 4.62.1.11 done / 🏦 Bank 2 — Capitalist metric `bank.total_interest_earned` (forward-only accumulator, += в `accrue_deposit`, не сбрасывается на снятии) tiers 100/500/1000/5000/10000 + Saver metric `bank.days_with_deposit` (+1 на rollover при депозите ≥1000) tiers 7/30/90/180/365, без seal, 4.62.1.10 done) / 🎒 Collection 6 — 5 per-type event-based (Ringbearer/Jeweler/Headhunter/Tailor/Cobbler по `item_type`) + Connoisseur (`quality>=90`), хуки drop/drop_pending/drop_force_sold (drop-only, зеркально Drops-per-grade), tiers 5/10/25/50/100, без seal, 4.62.1.14) + 7 seals (Marathoner / Indefatigable / Globetrotter / Polymath / Workaholic / Veteran ⭐ / Treasure Hunter 💎). **NB Veteran:** тиры 15-30 пока недостижимы (level-кэп = 12, `level.py`), seal Progression ждёт расширения уровней (TASKS 4.64, отложено). bank / collection заполнены; lifestyle отменена (4.62.1.13). Прочие задачей 4.62.1.x (см. TASKS.md «Phase 2 Catalog»). **Pinned (4.62.4):** игрок выбирает ≤3 triumph'а для приоритетного показа в status_bar, smart replace prompt при попытке pin 4-го. **Claim mechanic (4.62.4):** Destiny-2 паттерн — newly-unlocked tier'ы попадают в `unclaimed_unlocks`, status_bar показывает banner «🎁 N закрыто: имя1, ...», игрок должен явно `[c]` собрать в detail view (или `[a]` собрать все). One-shot backfill в `init_game_state` для existing players через `backfill_unclaimed_from_existing` (synth entries для уже-unlocked tier'ов). **Seals & Titles (4.62.3):** один seal per category, unlock при capstone'е всех triumph'ов категории, кладёт право надеть title (один за раз). `👑 <title>` рендерится в status_bar над «Вы находитесь в локации» если `state.title` non-None. **Engine API:** `register_event → list[unlocks]`, `init_metric_check`, `get_progress`, `total_score`, `_format_progress_bar (width=15, equal-sized segments)`, `append_unclaimed/claim_triumph (batch)/claim_one_tier (per-tier, since 0.2.5z)/next_unclaimed_tier (UI helper)/claim_all/get_unclaimed_for/backfill_unclaimed_from_existing`, `is_seal_unlocked/available_seals/available_titles/set_title/check_seal_unlocks`, `backfill_from_history(state, path)` / `backfill_from_sheets_history(state)` (cross-device через `HistoryLogRepo.since(0)`, since 0.2.5x / 4.62.6 — shared `_replay_events_into_counters` helper включает Iron Worker max-tracker). Glyphs: `▰` filled / `▱` empty / `│` tier separator. **CLI access:** команда `t` (или RU `е`) → `open_triumphs_menu(state)`. **Web access (since 0.2.5y / 4.62.7, UX-polished в 0.2.5z / 4.62.7.1):** 3 слоя UI в `web/templates/_status_fragment.html` — (1) top banners над Stats: unclaimed `🎁 N закрытых не собрано` + `[✓ Собрать все]` + `×` dismiss-на-сегодня (4.62.7.6 / 0.2.6a — **серверный**: × → HTMX `POST /web/triumphs/dismiss_unclaimed` ставит `state.unclaimed_banner_dismissed_date = date_last_enter` + persist; `_build_triumphs_view.has_unclaimed` = `есть unclaimed AND dismissed_date != date_last_enter`; на rollover баннер сам возвращается. Заменил client-side localStorage 4.62.7.5 — ненадёжен на iPhone Safari ITP), pinned `📌 Pinned N/3` с 3 строками progress; (2) title badge `📊 Stats  👑 <title>` в Stats header (float-right); (3) main `<section id="triumphs">` **в самом конце dashboard'а** после Inventory (gameplay-блоки приоритетнее visible сверху — перемещена в 0.2.5z после live testing) с sub-collapsibles per category (pin/claim buttons inline) + Seals sub-section + `🌐 Backfill из Sheets` button. View builder `_build_triumphs_view(state)` в `web/main.py` + 10 endpoints (5 web Form HTMX + 5 API JSON Pydantic: pin / claim / claim_all / seal_toggle / backfill_sheets, все с STALE handling). Pin cap UX = disabled button + tooltip (smart-replace остаётся CLI-only). **Per-tier claim semantic (4.62.7.1 / 0.2.5z):** `[✓ Собрать (N)]` button → `[✓ Собрать tier N (M ост.)]` — клик claim'ит **один** oldest tier (по `unlocked_ts ASC, tier ASC`), не batch'ает. Реализовано через новый engine helper `claim_one_tier(state, id, kind) -> Optional[dict]` + `next_unclaimed_tier(state, id, kind) -> Optional[int]` (UI label). Batch `[a] Собрать всё` в banner остался для quick-clear (через `claim_all`). **Category counts (4.62.7.2/3 / 0.2.6):** category sub-collapsible label = **claimed tiers / total tiers** (sum `current_tier - unclaimed_count` per triumph), не triumph count. Marathoner tier 3/5 со всеми 3 unclaimed → `🏃 Шаги · 0/5 ✨`; после claim_all → `3/5`. Top header `Score · X/Y tiers · Z/M seals` тоже использует claimed sum (раньше показывал «N/5 категорий» — misleading определение). **Score = claimed tiers (0.2.6 fix):** `triumphs.total_score` начисляет очки только за **собранные** tier'ы (`Σ max(0, tier − unclaimed_count) × points_per`) — раньше считал по unlocked tier и рассинхронивался с `X/Y tiers` counter'ом (Score 210 при 0/196 claimed). Теперь несобранный unlock даёт 0 очков, claim начисляет — Destiny-2 семантика. Seals в score НЕ входят (отдельный `Z/M seals` counter). **Synth-backfill anti-loop (4.62.4.1 fix / 0.2.6):** marker `state.triumphs['__synth_done__']['done']` ставится после первого `backfill_unclaimed_from_existing` — prevents infinite re-population queue после `claim_all` на каждом restart'е сервера. **CRITICAL persist after init backfill:** `characteristics.init_game_state` зовёт `save_characteristic` если synth добавил entries (без этого первый GET / → `try_reload_state.update_from_dict(Sheets)` overrides RAM значением [] из Sheets → 17 backfilled entries silently теряются). **Work tracking semantics design choice:** Hard Worker / per-vacancy считают часы на **завершении** смены (`work_done` event), не на старте. Симметрично с Energy: Workhorse/Endurance hook'аются на `work_start`+`work_extend` (энергия = факт в момент траты), Hard Worker — на финиш (часы = accumulator за время смены). **Bonuses на capstones** — отложено в 4.62.2.1 (deferred 25.05.2026 — нужен balance design; сейчас capstone даёт только score points, не gameplay-эффект). **Тесты:** `tests/test_triumphs.py` (engine + claim + seals + Sheets backfill + anti-loop synth flag ~83), `tests/test_triumphs_menu.py` (CLI UI + claim ~25), `tests/test_triumphs_catalog.py` (per-category ~103 — TestMarathoner 9 / TestEnergyTriumphs 12 / TestAdventureTriumphs 12 / TestGymTriumphs 14 / TestWorkTriumphs 12 / TestIronWorker 7 / TestVeteran 7 / TestDropTriumphs 7 / TestWayfarer 4 / TestInvestor 5 / TestDedicated 4 / TestOnFire 4 / TestRestorer 6 / TestCrafter 6 / TestCapitalist 9 / TestSaver 9 / TestCollection 8 — 0.2.6i). Web tests в `tests/test_web_main.py::test_*triumph*/pinned/unclaimed/title* ~13. Total в triumph stack ~187. Autouse fixture отключает `register_event` для non-triumph тестов (см. ниже).
- `report.py` (since 0.2.5e / task 4.2, 21.05.2026) — «Пока тебя не было» summary при входе в игру. Pure helpers: `INTERESTING_EVENT_TYPES` frozenset (7 passive event types — work_done / skill_upgraded / adventure_done / drop / drop_auto_collected / drop_force_sold / level_up / new_day; **player actions** типа skill_alloc / bank_* / item_repaired НЕ включены — игрок сам это делал), `build_away_report(since_ts) -> list[dict]` (читает Sheets history через `HistoryLogRepo.since`, фильтрует interesting; silent-fail если Sheets unavailable), `format_report_cli(events, since_ts) -> str` (colorama-rich CLI блок с unicode рамкой ═), `build_report_view(events, since_ts) -> dict` (pre-computed для web template — `has_events`, `items` list, `since_dt`, `elapsed_label`, `count`), `format_timedelta_simple(seconds) -> str` (human-readable «5 мин» / «2 ч 30 мин» / «1 дн 5 ч»). **Period:** `state.timestamp_last_enter` capture'ится в `init_game_state` как `prior_ts`. Since 0.2.5r / task 4.2.1 (22.05.2026) семантика поля = **«last activity / last save time»**, а не «session start» — `save_characteristic` обновляет его на `now()` перед каждым Sheets save (CLI mutation + web POST + auto-finalize). До 0.2.5r `init_game_state` overwrite'ил поле на `now()` при CLI startup, но web сессии marker не двигали (`try_reload_state` восстанавливал значение из Sheets) — после активного web-вечера CLI наутро показывал window от вчерашнего CLI startup. Теперь web activity корректно отражена. **Source:** Sheets `history` лист (полная картина CLI + web events; local `history.jsonl` per-machine и не покажет cross-client events). **Storage:** runtime-only поля `state.startup_report: list[dict]` + `startup_report_since_ts: float` (НЕ сериализуются). **Display:** CLI печатает в `game.py:play()` перед main loop'ом и очищает list (однократно за session); web рендерит banner через `_status_fragment.html` (azure border-left, header «🕒 Пока тебя не было», meta + ul) и очищает после первого render'а в `_build_away_report_view`. **Cost:** +1 Sheets API call (~500-700 мс) в init_game_state — для web one-time на uvicorn startup, для CLI один раз per session. **Trigger:** только если >= 1 interesting event — пустой report не показывается. **CLI просмотрщик истории (4.6.2, since 0.2.6):** команда `h` (RU `р`) → `open_history_viewer(state)` — интерактивный просмотр с пагинацией (страницы newest-first, `m`/Enter — следующая, `1`-`9` — размер страницы ×10, `0` — выход; хелперы `_print_history_page` + `_event_timestamp_label`). `read_recent_history(limit=None→все)` — Sheets `history` primary (`HistoryLogRepo.since(0)`, полная картина) + fallback на локальный `history.jsonl` при сетевой ошибке. Дата строки берётся из `ts` (Sheets-`since` не отдаёт отдельные date/time — только ts+datetime). Форматтер событий унифицирован: единое ядро `_event_emoji_text(event) -> (emoji, text)` покрывает все ~30 типов (work/skill/adventure/drop*/bank/item*/loadout/preset/new_day/steps_set/save/sync_conflict + generic fallback); `_format_event_cli` (colorama, per-category цвет через `_event_color`) и `_format_event_web` — тонкие обёртки над ядром. **NB (фикс 4.6.2):** drop-события читаются из **плоского** payload (`p.get('grade')` и т.д., как реально пишет `log_event`), а не nested `p.get('item')` — до фикса away-report рендерил дроп как «🎁 Дроп: None ?».

### Type hints

Since 0.2.1j-r (task 5.1, 05.05.2026) the codebase has ~100% type hint coverage across all modules — `state.py`, `actions.py`, `bonus.py`, `equipment_bonus.py`, `skill_bonus.py`, `level.py`, `characteristics.py`, `gym.py`, `work.py`, `adventure.py`, `inventory.py`, `equipment.py`, `shop.py`, `functions.py`, `functions_02.py`. Since 0.2.1u (task 5.6, 02.05.2026) **mypy подключён** — конфиг в `mypy.ini` с пермиссивным baseline (`disallow_untyped_defs=False`, `strict_optional=False`, `ignore_missing_imports=True`), запуск через `mypy .`, на момент подключения 0 ошибок. CI step пока не настроен (нет CI вообще), запускается локально перед коммитами. New code should add hints by default. Pattern: parameters `state: GameState`, return types explicit (`-> bool`, `-> None`, `-> tuple[X, Y]`). `Optional[X]` from `typing` for nullable params; `dict[str, int]` / `list[dict]` parameterized where structure is known. Item dicts (`state.inventory`, `state.equipment.<slot>`) typed as plain `dict` — TypedDict postponed to task 1.6 (Items as dataclass).

Один из побочных эффектов подключения mypy — обнаружен и исправлен реальный runtime-баг в `state.AdventureSession.end_ts` (5.6.1): тип был `Optional[datetime]`, а на запись/чтение всегда использовался Unix timestamp (`float`). `_deser_datetime(float)` возвращал `None`, поэтому `adventure_end_timestamp` терялся при каждом load — игрок терял прогресс активного приключения после reload/restart. Тип переведён на `Optional[float]`, `from_dict` читает значение напрямую без `_deser_datetime`. Урок: type-mismatch не всегда косметика — иногда это потерянное состояние.

### Tests

`tests/` contains pytest suites for each module. Pure logic helpers (`_sort_inventory`, `_buy_item`, `_equip_from_inventory`, `try_spend`, formula functions, `_state_dict_to_rows`, `_rows_to_state_dict`) are tested directly with hand-built `GameState` instances. UI methods that use `input()`/`print()` are tested via `capsys` and `monkeypatch.setattr('builtins.input', ...)`. Sheets repos (`GameStateRepo`, `StepsLogRepo`) are tested with mocked gspread (`unittest.mock.MagicMock`) — no real network. Tests do not depend on Google Sheets at all after task 1.2 (`import characteristics` is a pure operation), so the full suite runs in ~1 sec.

**Autouse-фикстуры в `tests/conftest.py` защищают production Sheets от загрязнения** (skip только для `test_sheets_repo.py` который тестирует сами repo + `test_history.py` который тестирует log_event):
- `_disable_history_log_for_non_history_tests` — `history.log_event` → no-op (иначе тесты пишут в реальный `history.jsonl` + Sheets `history` sheet).
- `_stub_steps_log_for_day` (since 0.2.4q + extended 0.2.4t) — `StepsLogRepo.for_day` → `[]` AND `StepsLogRepo.append` → no-op. Без `append`-мока тесты POST'ящие `/web/steps` или `/api/steps` пишут в production `steps_log` лист → max-merge на следующем load подтягивает test-значения как actual `state.steps.today` (реальный инцидент 19.05.2026: тест с `value=5100` загрязнил утренний gameplay игрока). Тесты которым нужно настоящее `append`-поведение (трекинг вызовов) переопределяют через `monkeypatch.setattr(StepsLogRepo, 'append', tracking_fake)` локально.
- `_disable_triumphs_register_event_for_non_triumphs_tests` (since 0.2.5k / task 4.62.0.2, 22.05.2026) — `triumphs.register_event` → `lambda state, event_type, **payload: []` для всех тестов кроме `test_triumphs.py` / `test_triumphs_catalog.py` (которые тестируют сам engine). Auto-hook `register_event` в `history.log_event` срабатывает на каждом mutation event'е во всех integration-тестах (web endpoint'ы / CLI flows) — без мока тесты бы мутировали `state.triumphs` дополнительными счётчиками и ломали fine-grained ассерты (например test_web_work_endpoint ожидает пустые triumphs после mutation, но получает unlock'нутый Workhorse).

### Credentials and ignored files

`.gitignore` excludes the `credentials/` directory (which must contain `2walks_service_account.json` for Sheets to work). When running locally for the first time, expect Sheets calls to fail until that file is provided; loaders fall back to CSV automatically (`characteristics.py:92`).
